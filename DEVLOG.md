# Dev Log

Auto-generated session log for StockSense / Rice Inventory MVP.

---


## Session: 2026-09-06 14:11
- **Branch:** main
- **Files changed:** none
- **New files:** .kiro/hooks/auto-devlog.json,.kiro/hooks/commit-reminder.json,.kiro/hooks/lint-on-save.json,.kiro/specs/mvp1-inventory-visibility/design.md,.kiro/specs/mvp1-inventory-visibility/requirements.md,.kiro/specs/mvp1-inventory-visibility/tasks.md,.kiro/steering/project-context.md
- **Notes:** _(add manual notes here)_

---

## Session: 2026-09-06 14:18
- **Branch:** main
- **Files changed:** none
- **New files:** none
- **Notes:** _(add manual notes here)_

---

## Session: 2026-09-06 14:22
- **Branch:** main
- **Files changed:** .kiro/specs/mvp1-inventory-visibility/design.md,.kiro/specs/mvp1-inventory-visibility/tasks.md,DEVLOG.md
- **New files:** none
- **Notes:** _(add manual notes here)_

---

## Session: 2026-09-06 14:23
- **Branch:** main
- **Files changed:** .kiro/specs/mvp1-inventory-visibility/design.md,.kiro/specs/mvp1-inventory-visibility/tasks.md,DEVLOG.md
- **New files:** none
- **Notes:** _(add manual notes here)_

---

## Session: 2026-09-06 14:26
- **Branch:** main
- **Files changed:** .kiro/specs/mvp1-inventory-visibility/design.md,.kiro/specs/mvp1-inventory-visibility/tasks.md,DEVLOG.md
- **New files:** none
- **Notes:** _(add manual notes here)_

---

## Session: 2026-09-06 14:29
- **Branch:** main
- **Files changed:** .kiro/specs/mvp1-inventory-visibility/design.md,.kiro/specs/mvp1-inventory-visibility/tasks.md,DEVLOG.md
- **New files:** none
- **Notes:** _(add manual notes here)_

---

## Session: 2026-09-06 14:42
- **Branch:** main
- **Files changed:** .gitignore,.kiro/specs/mvp1-inventory-visibility/design.md,.kiro/specs/mvp1-inventory-visibility/tasks.md,DEVLOG.md,backend/package-lock.json,backend/package.json,backend/src/index.js,frontend/src/components/Badge.jsx,frontend/src/components/Sidebar.jsx,frontend/src/pages/Alerts.jsx,frontend/src/pages/Dashboard.jsx,frontend/src/pages/Inventory.jsx
- **New files:** backend/src/db/init.js,frontend/src/mock/alertsData.js,frontend/src/mock/riceData.js,frontend/src/mock/statsData.js
- **Notes:** _(add manual notes here)_

---

## Session: 2026-09-06 14:49
- **Branch:** main
- **Files changed:** .gitignore,.kiro/specs/mvp1-inventory-visibility/design.md,.kiro/specs/mvp1-inventory-visibility/tasks.md,DEVLOG.md,backend/package-lock.json,backend/package.json,backend/src/index.js,frontend/src/components/Badge.jsx,frontend/src/components/Sidebar.jsx,frontend/src/pages/Alerts.jsx,frontend/src/pages/Dashboard.jsx,frontend/src/pages/Inventory.jsx
- **New files:** backend/src/db/init.js,frontend/src/mock/alertsData.js,frontend/src/mock/riceData.js,frontend/src/mock/statsData.js
- **Notes:** _(add manual notes here)_

---

## Session: 2026-09-06 14:59
- **Branch:** main
- **Files changed:** .gitignore,.kiro/specs/mvp1-inventory-visibility/design.md,.kiro/specs/mvp1-inventory-visibility/tasks.md,DEVLOG.md,backend/package-lock.json,backend/package.json,backend/src/index.js,frontend/src/components/Badge.jsx,frontend/src/components/Sidebar.jsx,frontend/src/pages/Alerts.jsx,frontend/src/pages/Dashboard.jsx,frontend/src/pages/Inventory.jsx
- **New files:** backend/src/db/init.js,frontend/src/mock/alertsData.js,frontend/src/mock/riceData.js,frontend/src/mock/statsData.js
- **Notes:** _(add manual notes here)_

---

## Session: 2026-09-06 15:03
- **Branch:** main
- **Files changed:** .gitignore,.kiro/specs/mvp1-inventory-visibility/design.md,.kiro/specs/mvp1-inventory-visibility/tasks.md,DEVLOG.md,backend/package-lock.json,backend/package.json,backend/src/index.js,frontend/src/components/Badge.jsx,frontend/src/components/Sidebar.jsx,frontend/src/pages/Alerts.jsx,frontend/src/pages/Dashboard.jsx,frontend/src/pages/Inventory.jsx
- **New files:** backend/src/db/init.js,frontend/src/mock/alertsData.js,frontend/src/mock/riceData.js,frontend/src/mock/statsData.js
- **Notes:** _(add manual notes here)_

---

## Session: 2026-09-06 15:18
- **Branch:** main
- **Files changed:** .gitignore,.kiro/specs/mvp1-inventory-visibility/design.md,.kiro/specs/mvp1-inventory-visibility/tasks.md,DEVLOG.md,backend/package-lock.json,backend/package.json,backend/src/index.js,frontend/src/components/Badge.jsx,frontend/src/components/Layout.jsx,frontend/src/components/Sidebar.jsx,frontend/src/components/StatCard.jsx,frontend/src/index.css,frontend/src/main.jsx,frontend/src/pages/Alerts.jsx,frontend/src/pages/Dashboard.jsx,frontend/src/pages/Inventory.jsx
- **New files:** backend/src/db/init.js,frontend/mockups/stock-position-concepts.html,frontend/src/context/ThemeContext.jsx,frontend/src/mock/alertsData.js,frontend/src/mock/riceData.js,frontend/src/mock/statsData.js
- **Notes:** _(add manual notes here)_

---

## Session: 2026-09-12 19:53
- **Branch:** main
- **Files changed:** .gitignore,.kiro/specs/mvp1-inventory-visibility/design.md,.kiro/specs/mvp1-inventory-visibility/requirements.md,.kiro/steering/project-context.md,backend/src/db/init.js,backend/src/db/seed.js,backend/src/engines/alerts.js,backend/src/engines/financials.js,backend/src/engines/health.js,backend/src/engines/index.js,backend/src/engines/position.js,backend/src/engines/safetystock.js,backend/src/engines/smoke.js,backend/src/index.js
- **New files:** .kiro/specs/mvp1-inventory-visibility/reference/rice-inventory-technical-spec.md,.kiro/specs/mvp1-inventory-visibility/reference/rice-inventory-terms-glossary.md,.kiro/specs/mvp1-inventory-visibility/reference/terminology-map.md,backend/src/engines/projection.js,backend/src/routes/inventory.js
- **Notes:** _(backfilled — see note at end of log)_ Backend: domain terminology alignment (TASK-15) + projected-inventory engine (TASK-07/09).

---

## Session: 2026-09-12 19:54
- **Branch:** main
- **Files changed:** frontend/src/api/inventory.js,frontend/src/components/FormField.jsx,frontend/src/components/Sidebar.jsx,frontend/src/components/StatCard.jsx,frontend/src/components/StockPositionBar.jsx,frontend/src/components/hintStyles.js,frontend/src/index.css,frontend/src/mock/alertsData.js,frontend/src/mock/analytics.js,frontend/src/mock/riceData.js,frontend/src/mock/statsData.js,frontend/src/pages/Alerts.jsx,frontend/src/pages/Dashboard.jsx,frontend/src/pages/Inventory.jsx
- **New files:** frontend/src/components/ErrorState.jsx,frontend/src/components/LoadingState.jsx
- **Notes:** _(backfilled)_ Frontend visual design overhaul (Linear-style, TASK-16) + live sidebar alert badge (TASK-13) + real projection chart wiring.

---

## Session: 2026-09-12 19:54
- **Branch:** main
- **Files changed:** .kiro/specs/mvp1-inventory-visibility/tasks.md
- **New files:** none
- **Notes:** _(backfilled)_ Docs: logged TASK-07, TASK-09, TASK-13, TASK-16 completion in tasks.md.

---

## Session: 2026-09-12 20:10
- **Branch:** main
- **Files changed:** .kiro/specs/mvp1-inventory-visibility/tasks.md,backend/src/db/seed.js,backend/src/engines/projection.js,frontend/src/pages/Dashboard.jsx,frontend/src/pages/Inventory.jsx
- **New files:** none
- **Notes:** _(backfilled)_ Bug-hunting pass (TASK-17): fixed a seed-script FK failure that permanently blocked reseeding after any real decision was recorded, a projection-engine blank-date bug, and a Dashboard/health-engine drift where a SKU could show "Healthy" and a critical exception at the same time.

---

## Session: 2026-09-12 21:37
- **Branch:** main
- **Files changed:** .kiro/specs/mvp1-inventory-visibility/tasks.md,frontend/src/pages/Alerts.jsx
- **New files:** none
- **Notes:** _(backfilled)_ Bug-hunting pass (TASK-18): Alerts decision-flow audit — Ask AI was showing a different SKU's canned explanation, Approve/Reject silently discarded input on a failed save, Reason field wasn't actually required despite its label.

---

## Session: 2026-09-12 21:51
- **Branch:** main
- **Files changed:** .kiro/specs/mvp1-inventory-visibility/tasks.md,backend/src/routes/inventory.js,frontend/src/pages/Inventory.jsx
- **New files:** none
- **Notes:** _(backfilled)_ Bug-hunting pass (TASK-19): Restock modal silently no-op'd on invalid input; added server-side numeric validation to the SKU routes (previously client-side only).

---

## Session: 2026-09-12 21:55
- **Branch:** main
- **Files changed:** .kiro/specs/mvp1-inventory-visibility/tasks.md,frontend/src/pages/Alerts.jsx
- **New files:** none
- **Notes:** _(backfilled)_ Bug-hunting pass (TASK-20): Escape key and background-scroll-lock now work consistently across all modals, not just the Inventory ones.

---

## Session: 2026-09-12 21:59
- **Branch:** main
- **Files changed:** .kiro/specs/mvp1-inventory-visibility/tasks.md,frontend/src/components/Sidebar.jsx
- **New files:** none
- **Notes:** _(backfilled)_ Bug-hunting pass (TASK-21): fixed a leftover hardcoded badge color that broke in Glass theme; code-reviewed the remaining backend engines (no bugs found).

---

## Session: 2026-09-12 22:31
- **Branch:** main
- **Files changed:** .kiro/specs/mvp1-inventory-visibility/tasks.md,frontend/src/index.css,frontend/src/pages/Dashboard.jsx
- **New files:** none
- **Notes:** _(backfilled)_ Dashboard redesign (TASK-22): three concept mockups reviewed via a published artifact before implementing; shipped Concept A ("Signal First") — merged three overlapping list widgets into one, health-by-value as a donut, ABC×XYZ behind a disclosure.

---

## Session: 2026-09-12 22:42
- **Branch:** main
- **Files changed:** .kiro/specs/mvp1-inventory-visibility/tasks.md,frontend/src/components/StatCard.jsx,frontend/src/pages/Dashboard.jsx
- **New files:** none
- **Notes:** _(backfilled)_ Dashboard redesign (TASK-23): switched to Concept B ("Command Deck") per user preference after seeing both live — real PowerBI-style click-to-filter from three charts into a table, ELI18 help tooltips on every metric, independently collapsible widgets persisted per-browser.

---

**Tooling note (2026-09-12):** the entries above were backfilled by hand — `auto-devlog.json`'s `"trigger": "Stop"` fires on a *Kiro* session ending, not a Claude Code one, so it never ran during this session despite 9 real commits landing. If you want logging to stay automatic while working in Claude Code, this hook needs a Claude Code–side equivalent (or an entry needs to be appended manually, e.g. via a `git commit` alias/hook) — it isn't happening on its own today.

## Session: 2026-09-12 23:11
- **Branch:** main
- **Files changed:** .kiro/specs/mvp1-inventory-visibility/tasks.md,frontend/src/pages/Alerts.jsx,frontend/src/pages/Dashboard.jsx
- **New files:** frontend/src/hooks/useCollapsed.js
- **Notes:** Alerts page polish (TASK-24): removed a redundant filter-tiles/filter-tabs duplication, added ELI18 help tooltips, made the Decision Log collapsible (shared hook extracted from Dashboard), restyled alert cards to drop the boxed/shadowed look, and corrected the AI modal's disclaimer copy to stop overstating what the current rule-based placeholder does.

---

## Session: 2026-09-12 23:45
- **Branch:** main
- **Files changed:** .kiro/specs/mvp1-inventory-visibility/tasks.md, DEVLOG.md, frontend/src/index.css, frontend/src/pages/Dashboard.jsx, frontend/src/components/StatCard.jsx, frontend/src/components/hintStyles.js, frontend/src/components/Sidebar.jsx, frontend/src/components/Badge.jsx, frontend/src/components/FormField.jsx, frontend/src/components/ErrorState.jsx, frontend/src/components/StockPositionBar.jsx, frontend/src/pages/Inventory.jsx, frontend/src/pages/Alerts.jsx
- **New files:** none
- **Notes:** Dashboard critic pass (TASK-25): ran a strict UI/UX + data-analyst review of the Dashboard, reported findings by severity, then fixed the Critical + High items the user picked — honest "vs baseline" relabel (was silently asserting a real month-over-month feed that doesn't exist), a site-wide `:focus-visible` outline (buttons had none at all), touch/keyboard-accessible chart tooltips (swapped native `title` for the existing `HoverHint` pattern on 3 widgets), recalibrated KPI status thresholds so "bad" is reachable (not just perpetual "warn"), un-hid Coverage-in-Target-Band (28.9% — a real alarm, not a duplicate metric), and fixed `buildNeedsAttention` silently dropping overlapping SKU conditions plus a latent filter-after-truncation bug. Also did a site-wide font-size increase (type scale + body floor to 16px, per WCAG/Apple HIG/Material guidance) for older users, sweeping hardcoded sub-12px literals across 9 components.

---

## Session: 2026-09-13 00:05
- **Branch:** main
- **Files changed:** CLAUDE.md (new), .kiro/steering/project-context.md, .kiro/specs/mvp1-inventory-visibility/tasks.md, DEVLOG.md, frontend/src/context/ThemeContext.jsx, frontend/src/index.css, frontend/src/components/Sidebar.jsx, frontend/src/components/Badge.jsx, frontend/src/components/hintStyles.js, frontend/src/pages/Inventory.jsx, frontend/src/pages/Dashboard.jsx, frontend/src/pages/Alerts.jsx, frontend/src/components/StatCard.jsx, frontend/src/components/StockPositionBar.jsx, frontend/src/components/FormField.jsx, frontend/src/components/LoadingState.jsx, frontend/src/components/HoverHint.jsx, frontend/src/hooks/useCollapsed.js, frontend/src/api/inventory.js, frontend/src/mock/*.js, backend/src/engines/alerts.js, backend/src/engines/index.js, backend/src/routes/products.js
- **New files:** CLAUDE.md
- **Notes:** Removed the Liquid Glass theme entirely (user called it useless) and stripped every em dash and en dash from UI copy and frontend code comments plus the backend's user-facing recommendation strings, following user feedback that heavy dash usage reads as an AI-generated tell. Wrote the "no em/en dash" rule into a new root CLAUDE.md and into the Kiro steering doc so it persists for future agent sessions on this project, not just this one.

---

## Session: 2026-09-13 00:20
- **Branch:** main
- **Files changed:** .kiro/specs/mvp1-inventory-visibility/tasks.md, DEVLOG.md, frontend/src/components/Sidebar.jsx, frontend/src/components/Layout.jsx, frontend/src/index.css
- **New files:** none
- **Notes:** Responsive nav (TASK-27): brainstormed three small-screen nav concepts (floating bottom bar, floating top bar, expanding FAB) via ASCII mockups, user picked the floating glass top bar. Sidebar.jsx now renders both the full side panel and a new compact frosted-glass bar; a 768px media query picks which one shows. Verified with a temporary CSS override since real window resizing does not change the actual viewport in this browser-automation environment.

---

## Session: 2026-09-13 00:15
- **Branch:** main
- **Files changed:** .kiro/specs/mvp1-inventory-visibility/tasks.md, DEVLOG.md, frontend/src/components/Sidebar.jsx, frontend/src/pages/Dashboard.jsx, frontend/src/index.css
- **New files:** none
- **Notes:** Fixed a real bug the user caught with a phone screenshot: the floating glass bar and the full side panel both rendered at once. Cause was an inline `display: flex` on the sidebar beating the breakpoint's `display: none`. The original verification had injected `!important`, which masked it, so it passed on broken code. Re-verified with no `!important` this time. Also moved the Dashboard's two widget rows off inline grid columns so they collapse to one column on small screens.

---

## Session: 2026-09-13 00:30
- **Branch:** main
- **Files changed:** .kiro/specs/mvp1-inventory-visibility/tasks.md, DEVLOG.md, frontend/src/index.css, frontend/src/pages/Dashboard.jsx, frontend/src/components/StatCard.jsx
- **New files:** none
- **Notes:** Second critic pass on the Dashboard (TASK-28). Restored white cards at the user's request (new .card class on --card-bg, applied to all four widgets plus a summary card around the hero and KPI strip). Caught a real data bug: Compliance Position is metric tonnes but was rendering through fmt$ as "+$1K" for what is actually +1,058 MT; added a separate fmtMt formatter. Swapped the widget rows so Needs Attention sits above the fold instead of the ABC x XYZ matrix. Made the hero delta neutral (its own hint says the direction is ambiguous, but it was painted red), reconciled gross vs risk-adjusted E&O, cut KPI colour overload to a status dot for "warn", labelled the month comparison chart, removed stretched dead space, added a heatmap legend, and fixed --text-muted failing WCAG AA (3.6:1 -> 4.76:1).

---

## Session: 2026-09-13 00:40
- **Branch:** main
- **Files changed:** DEVLOG.md, frontend/src/api/inventory.js
- **New files:** none
- **Notes:** Made a dead backend diagnose itself. Previously any failure collapsed to "Request failed (500)", which points you at the frontend when the real problem is that the API process is gone. Probed the actual failure mode first: with :4000 down, the Vite proxy answers with a bare text/plain 500 and an empty body, while every genuine Express error returns JSON with a message. That makes an unparseable body a reliable signal the request never reached Express, so the client now shows "Cannot reach the API on localhost:4000..." for that case and leaves real API messages untouched. Verified all three paths: backend down (browser), genuine 404 (keeps "SKU not found"), and Retry recovering once the backend is back.

---

## Session: 2026-09-13 00:50
- **Branch:** main
- **Files changed:** DEVLOG.md, frontend/src/index.css, frontend/src/pages/Dashboard.jsx, frontend/src/components/HoverHint.jsx, frontend/src/components/ColHint.jsx
- **Deleted:** frontend/src/components/hintStyles.js
- **Notes:** Three UI changes. (1) Removed justifyContent:space-between from the hero row so the Baseline/Now chart sits tight against the number it explains instead of being pinned ~700px away at the card edge. (2) Grouped the seven KPIs into two labelled rows along the project's own two competing objectives (service/availability vs working capital), sharing one 4-column grid so cards align between rows. (3) Rebuilt the hint tooltip as theme-aware liquid glass: it was one light panel with dark text in both themes, measuring 4.04:1 over a dark backdrop (under AA), which is why it could not be pushed glassier. Light tint/dark text in Light and dark tint/light text in Dark now measures 7.8-17.9:1 and 5.9-13.9:1 while running a lower fill alpha, plus gradient fill, specular top edge, inner base shade and a prefers-reduced-transparency fallback.

---

## Session: 2026-09-13 01:05
- **Branch:** main
- **Files changed:** DEVLOG.md, frontend/src/index.css
- **New files:** none
- **Notes:** Applied the user's own glass values from the Glass Tooltip Tuner artifact (published this session at claude.ai/code/artifact/62c1d4c6). Light theme taken as given: fill 0.72/0.33, ring 0.09, drop 0.74, blur 3px saturate 220%, radius 15px, gradient 135deg. Measured worst case 17.85:1 on card, 10.1:1 over green, 7.1:1 over red, all still AAA. Did NOT take the dark fills from the same paste (0.44/0.34): those ride along with the tuner's "Max glass" preset and measure 3.74:1 over a bright health segment, under AA, where the shipped 0.66/0.52 holds 5.58:1. User said they mainly tuned light and care about light, so dark was left safe and the reason recorded in a comment.

---

## Session: 2026-09-13 01:25
- **Branch:** main
- **Files changed:** .kiro/specs/mvp1-inventory-visibility/{tasks,design,requirements}.md, DEVLOG.md, backend/src/engines/{segmentation,index,smoke}.js, frontend/src/pages/Dashboard.jsx
- **New files:** none
- **Notes:** Replaced the ABC x XYZ matrix with ABC x movement class (TASK-29), after the user asked whether the chart was in the documentation. It was not: ABC is specified in technical-spec Step 8A, but XYZ appears in no source domain doc, and Step 8A item 7 actually asks to "Combine ABC class with Fast/Normal/Slow/Idle". The code shipped first (c17ae80) and REQ-14/design.md were back-filled to describe it, which is how XYZ came to look specified. It was also the weakest widget on real data: 5 of 9 cells empty including both AZ and BZ, the cells its own caption told you to act on. New matrix populates 6 of 12 along a clean diagonal. Also corrected design.md and REQ-14 rather than leaving the drift, and renamed stats.abcXyzMatrix to stats.abcMovementMatrix.

---

## Session: 2026-09-13 02:00
- **Branch:** main
- **Files changed:** .kiro/specs/mvp1-inventory-visibility/tasks.md, DEVLOG.md, backend/src/engines/{alerts,index}.js, frontend/src/pages/{Inventory,Alerts}.jsx, frontend/src/components/{StockPositionBar,FormField}.jsx
- **New files:** none
- **Notes:** Full interactive audit of all three pages (TASK-30), 10 fixes. Biggest: the stock bar drew the SUGGESTED reorder point while the status badge and alerts used the POLICY one, so a SKU could show a red "28 MT below reorder point" bar beside a green "Healthy" badge, and the tick jumped when switching modal tabs. All bars now use policy, suggested named separately in the hover. Also: "Purchase 0 MT" on alerts that mean stop buying, a clipped chart Y-axis, "1 active alerts", premature validation scolding on modal open, the modal re-centring and sliding its tabs on every switch, Save offered on a read-only tab, misaligned Min order qty, full-weight zero tiles, unlabelled filter selects, and one carrying-cost figure formatted two different ways in two places.

---

## Session: 2026-09-13 02:20
- **Branch:** main
- **Files changed:** .kiro/specs/mvp1-inventory-visibility/tasks.md, DEVLOG.md, README.md, backend/src/db/seed.js, backend/src/routes/inventory.js, frontend/src/App.jsx, frontend/src/api/inventory.js, frontend/src/components/Sidebar.jsx
- **New files:** backend/src/db/audit.js, frontend/src/pages/Activity.jsx
- **Notes:** TASK-31 and TASK-32, chosen after auditing what actually stands between the repo and a submission. Two gaps mapped straight onto scored criteria. First: audit_log had been in the schema from day one with a comment naming the events it should hold, and nothing ever wrote to it, while Observability is an explicit judging criterion. Now six real write points, a field-level before/after diff on policy edits, the system proposal stored next to the manager's action on decisions, GET /api/audit, and a new Activity page rendering it as plain-English sentences with the raw payload one click away. ALERT_TRIGGERED deliberately fires inside the dedupe_key branch so it means "condition first became true", not "page was loaded", and seed.js now wipes audit_log with alerts_log or the trail would stay empty forever after a reseed. Second: the README still described the pre-rebuild app (categories, out of stock, top-selling products, in-memory store), which is what a judge reads first. Rewritten around the reasoning loop with a diagram, the domain-rules table, the real API, and an honest statement that TASK-11 is blocked on a key rather than on design. Three defects caught in browser verification, including ColHint rendering only its icon so the page shipped with no visible h1.

---

## Session: 2026-09-13 02:45
- **Branch:** main
- **Files changed:** .kiro/specs/mvp1-inventory-visibility/tasks.md, DEVLOG.md, SUBMISSION.md, backend/src/db/init.js, backend/src/index.js
- **New files:** SUBMISSION.md
- **Notes:** TASK-33. Audited the submission rather than the code and found the real risk is not polish: three of the four required artifacts (deployment URL, demo video, PDF) do not exist, and only the deployment has an external dependency that cannot be rescued late. Did every part of it that does not need a host. DATA_DIR and CORS_ORIGIN are now env driven, the backend serves the built frontend with a SPA fallback under NODE_ENV=production, and an empty database seeds itself once on boot. Verified by booting a production server against an empty temp directory: created the dir, seeded, served the app, survived a hard refresh on a deep link, and still returned JSON for a bad API path, with local dev untouched. Also wrote SUBMISSION.md tracking all four deliverables, with the Render settings, a beat-by-beat demo video script built around the reasoning loop rather than a feature tour, and a PDF outline that assembles from existing prose instead of rewriting it.

---

## Session: 2026-09-13 04:30
- **Branch:** main
- **Files changed:** .kiro/specs/mvp1-inventory-visibility/tasks.md, DEVLOG.md, SUBMISSION.md, frontend/src/components/StatCard.jsx, frontend/src/components/StockPositionBar.jsx, frontend/src/pages/Dashboard.jsx
- **New files:** render.yaml, Dockerfile, .dockerignore, WRITEUP.md
- **Notes:** Overnight run, TASK-34 through TASK-36. Deployment blueprint written (render.yaml plus a Dockerfile fallback); the image could NOT be build-tested because no container runtime is installed on this machine, so what was verified instead is the layout and runtime contract it depends on, by assembling the image's directory structure in a temp dir and booting it with an empty DATA_DIR. Four deferred defects fixed, three verified on screen. The Japonica bar was worse than reported: idle fill, track and avoid-shading were all near-identical grey so the bar read as a disabled control, and because idle short-circuited the over-maximum branch the bar also stayed silent about 18 MT of overage that the Dashboard was simultaneously tagging as OVERSTOCK. Hatched fill plus a status line carrying both facts. The Needs Attention re-ranking could not be shown on screen because the seed produces no REORDER row; verified by inspection only, and that gap is recorded rather than glossed. PDF write-up drafted by assembling existing prose. Nothing committed, per instruction.

---

## Session: 2026-09-13 06:10
- **Branch:** main
- **Files changed:** .kiro/specs/mvp1-inventory-visibility/tasks.md, DEVLOG.md, backend/src/engines/alerts.js, frontend/src/pages/Alerts.jsx, frontend/src/pages/Dashboard.jsx
- **New files:** frontend/src/lib/explain.js
- **Notes:** TASK-37. Set out to improve the Ask AI modal and found a data bug on the way. The suggested order quantity was being computed two different ways: engines/index.js used the spec-correct projected-at-lead-time formula while alerts.js and the Dashboard both still used the snapshot proxy that design.md records as retired on 2026-09-12. They disagreed by 257 MT on TJ-25KG, about $347K, with the Inventory page showing one figure and the Alerts approval modal pre-filling the other. Three sites now read the engine's own number. Then the original target: Ask AI concatenated the alert message with the recommended action, both already printed on the card behind it, so the app's single AI touchpoint returned the card to the reader. Replaced with lib/explain.js, a four-step reasoning trace per alert type built from the enriched SKU (measured, derived, consequence, trade-off), rendered as a numbered rail. The trace doubles as the prompt context for TASK-11 whenever a key arrives. Also aligned three money formatters that disagreed on the same figures, and fixed a decision footer that said "No order required for this alert" beside three decision buttons.

---

## Session: 2026-09-13 06:50
- **Branch:** main
- **Files changed:** .kiro/specs/mvp1-inventory-visibility/tasks.md, DEVLOG.md, frontend/src/lib/explain.js
- **New files:** none
- **Notes:** TASK-38. Exercised all six explanation branches against all ten SKUs (240 sections) instead of clicking modals, which is both cheaper and far better coverage. The interesting find was a bug class rather than a bug: Number(null) is 0 and finite, so the Number.isFinite guards were rendering missing values as a confident "0 MT" rather than failing visibly, which a grep for "null" can never detect. Hardened the helpers, which then exposed two sentences that divide by demand and break on zero-demand SKUs. Reading the prose of the branches never seen on screen also caught a correctness bug no automated check would find: the OVERSTOCK explanation called a Normal mover with real daily sales "unlikely to clear on demand alone", which would push a manager toward an unnecessary discount. Narrow-viewport layout on the Activity page remains unverified: the browser resize tool reports success but leaves the viewport unchanged, and that is recorded rather than assumed away.

---

## Session: 2026-09-13 10:35
- **Branch:** main
- **Files changed:** .kiro/specs/mvp1-inventory-visibility/tasks.md, DEVLOG.md, SUBMISSION.md, WRITEUP.md
- **New files:** docs/images/{alerts-human-in-the-loop,reasoning-trace,activity-audit-record}.jpg
- **Notes:** TASK-39. Captured the write-up's screenshots from the running app rather than leaving placeholder markers, so the PDF now needs only the deployment URL. Added a third image the outline did not ask for: section 3 spends four paragraphs arguing the deterministic-first case and had no evidence on the page, and the reasoning trace is both that evidence and the best-looking screen in the build. Captions are written to carry the argument rather than to describe the picture.

---

## Session: 2026-09-13 11:20
- **Branch:** observability-and-submission-prep
- **Files changed:** .kiro/specs/mvp1-inventory-visibility/tasks.md, DEVLOG.md, WRITEUP.md, docs/images/alerts-human-in-the-loop.jpg, frontend/src/pages/Alerts.jsx
- **New files:** none
- **Notes:** TASK-40. Stan asked to declutter Alerts and whether Activity should fold into it; the answers connect. The clutter was structural: four encodings of two facts (stripe, icon, type chip, severity chip, over a tile row already grouping by type), a recommended-action panel boxed inside an already-bordered card, actions split between a floating Dismiss and a footer that only some cards had, and four differently-coloured buttons with no hierarchy. Severity moved to the stripe, the panel unboxed, one action row on every card, one primary button that names what it records. Also made every alert decidable: needsApproval had been hiding the decision controls on four of seven live alerts whose recommended actions are perfectly decidable, while the write-up claimed the opposite. On the second question: Activity stays standalone, because Alerts is a queue that shrinks as you work and Activity is a record that only grows, and Activity covers restocks and policy edits that are not alerts at all. The genuine duplication was the Decision Log table at the bottom of Alerts, a worse rendering of the audit trail's own DECISION_RECORDED rows, so it became a one-line pointer to /activity.

---

## Session: 2026-09-13 11:45
- **Branch:** observability-and-submission-prep
- **Files changed:** .kiro/specs/mvp1-inventory-visibility/tasks.md, DEVLOG.md, docs/images/alerts-human-in-the-loop.jpg, frontend/src/pages/Alerts.jsx
- **New files:** none
- **Notes:** TASK-41. Stan pointed out the alert content was reading against the grey page background. He was right and it was an inconsistency rather than a preference: every other surface in the app (dashboard widgets, inventory table, activity timeline, and the alert tile row directly above) sits on .card, and the alert list alone sat on bare --bg. Each alert is now its own card rather than the list being one surface, because an alert is a single decision and dismissing it removes exactly one card. Also removed the per-row bottom border, which was redundant with the parent's 14px gap, and added overflow hidden so the 3px severity stripe does not square off the rounded corners.

---

## Session: 2026-09-13 13:10
- **Branch:** main
- **Files changed:** .kiro/specs/mvp1-inventory-visibility/tasks.md, DEVLOG.md, backend/src/routes/inventory.js, frontend/src/api/inventory.js, frontend/src/pages/Alerts.jsx
- **New files:** backend/.env.example, backend/src/llm/provider.js, backend/src/llm/explain.js
- **Notes:** TASK-11 unblocked, and not by finding a key. Built a provider seam with four settings (ollama, gateway, anthropic, none) for a budget reason: the team shares one $100 pool with no per developer limit. Then Stan surfaced the organizers' starter kit, whose Bedrock gateway turns out to be deliberately Ollama compatible, so supporting it was a refactor rather than a rewrite and the seam paid off for a reason it was not built for. Most of that starter kit is solving tool-calling problems StockSense does not have, so it is deliberately not adopted. The model narrates and never computes: a llama3 test mislabelled margin as sales, which is why the facts block names the unit on every figure. Stan chose bucketed caching, and writing a test of his chosen behaviour immediately caught a bug in my first key, which included figures derived from available_qty and so gave one stock movement three chances to bust the cache.

---

## Session: 2026-09-13 14:05
- **Branch:** main
- **Files changed:** .kiro/specs/mvp1-inventory-visibility/tasks.md, DEVLOG.md, backend/.env.example, backend/src/db/audit.js, backend/src/llm/provider.js, backend/src/routes/inventory.js, frontend/src/api/inventory.js, frontend/src/components/Sidebar.jsx
- **New files:** frontend/src/components/AiModeSwitch.jsx
- **Notes:** TASK-42. Stan asked for free-by-default with the paid tap behind an explicit action, plus a visible indicator, after confirming the $100 is an AWS credit. Three tiers now, and the guardrails are structural rather than advisory: LLM_DEFAULT_MODE cannot select the metered tier at all, so paid spending can only start from a click in the running app and nobody inherits a billing state from a copied .env; switching to cloud without credentials is refused instead of accepted-then-failing; and the switch itself takes two clicks with the cost marked before the first one. Also declined to read the AWS PDF Stan attached, on the grounds that pulling credentials into the transcript through a file attachment is the same risk as pasting them in chat (he later clarified the keys are in the email, not the PDF). Terminology: rejected "hard-coded answers" for the no-AI tier because nothing is stored, it is computed from live figures by documented rules, and the phrase undersells it.

---

## Session: 2026-09-13 15:40
- **Branch:** main
- **Files changed:** .kiro/specs/mvp1-inventory-visibility/tasks.md, DEVLOG.md, backend/.env.example, backend/src/llm/explain.js, backend/src/llm/provider.js
- **New files:** backend/scripts/bench-models.js
- **Notes:** TASK-43. Hardened the prompt, then added the half that does not rely on the model cooperating: a deterministic verifier that checks every figure in the prose against the figures supplied. Pulled llama3.1:8b and qwen3:8b to compare. The first benchmark reported qwen3 at 7/7 clean and it was completely wrong: qwen3 is a reasoning model, spent its capped token budget on internal monologue, and returned nothing at all, and a verifier that only looks for bad things finds none in an empty string. Fixed by sending think:false (which also made it 36% faster) and by failing responses under 80 chars. Also nearly shipped a 1% numeric tolerance that passed 26000 for 26217. With 28 runs per model, llama3 that Stan already had wins on both accuracy and speed and is the only one with zero dangerous drift, so the two downloads produced evidence rather than a new model. Mid-edit I also destroyed explain.js by slicing between two string indices and deleting buildFacts and cacheKey with it; recovered from HEAD and redid it with anchored insert-only replacements.

---

## Session: 2026-09-13 15:55
- **Branch:** main
- **Files changed:** DEVLOG.md, backend/scripts/bench-models.js
- **New files:** none
- **Notes:** Removed qwen3:8b after the benchmark (slowest, and the only model that repeatedly renamed a money figure as "sales"). Kept llama3.1:8b at Stan's call, which is the better instinct: its weaker score came mostly from one alert it failed 4 of 4 times, a systematic and fixable trigger rather than general unreliability, so it is worth re-testing after the JP-5KG facts fix. llama2 stays, Stan uses it for another project. Also updated the benchmark's default model list, which still named qwen3:8b and would have made a no-args run fail for the next person.

---

## Session: 2026-09-13 16:50
- **Branch:** main
- **Files changed:** .kiro/specs/mvp1-inventory-visibility/tasks.md, DEVLOG.md, backend/scripts/bench-models.js, backend/src/engines/alerts.js, backend/src/engines/index.js, backend/src/llm/explain.js, frontend/src/lib/explain.js, frontend/src/pages/Dashboard.jsx
- **New files:** backend/src/engines/duration.js
- **Notes:** TASK-43 continued plus TASK-44. Implemented the retry policy (dangerous drift is retried then rejected, hedging is accepted immediately: 21 hedges in one run all had correct figures, so retrying them would spend a paid call to delete the word "nearly"). Split the benchmark's score into exact, cosmetic and DANGEROUS, which immediately revealed that the facts fix HAD worked: llama3.1's dangerous drift fell 6 to 2 and stayed there, hidden until then behind a single blended percentage that rated inventing a figure and writing "nearly" as the same event. Then Stan asked for "9 months and 8 days" instead of decimals, which was also a latent bug since my new line said 9.1 while the alert said 9.3. Fixed by computing the string once in the engine. Three bugs found on the way: Number(null) rendering a null cover as "0 days", a circular require that warned instead of throwing, and a grammar leak. Honest result: the readable format made year conversions worse, because "9 months and 8 days" invites "nearly a year" in a way "278 days" did not, so the rule of supplying information instead of forbidding behaviour does not generalise. Kept it anyway, readability was the point and the reject-and-fall-back path means no wrong number reaches the screen.

---

## Session: 2026-09-13 18:05
- **Branch:** main
- **Files changed:** .kiro/specs/mvp1-inventory-visibility/tasks.md, DEVLOG.md, backend/src/llm/explain.js, backend/src/routes/inventory.js, frontend/src/pages/Alerts.jsx
- **New files:** backend/src/llm/slots.js
- **Notes:** TASK-45, and it came from Stan's question rather than from me: if the maths is hard coded, why is the model touching numbers at all. He was right that it should not be. The engines always computed the figures, but the model retyped them, and every numeric failure measured today happened in the retyping. Now it writes prose with named placeholders and is rejected if it emits any digit, so a wrong figure is unrepresentable rather than merely detectable. The interesting consequence is that constraining the arithmetic relocated the risk to the argument: on the IDLE alert the model wrote "place an order" with perfectly correct numbers, which is the opposite of the right action and reads as authoritative because the figures check out. Fixed with a digit free semantic brief per alert type. Measured 21 explanations end to end: 14 slots, 7 free text, 0 rejected, 0 dangerous. Honest caveat recorded: on an 8B model slot mode costs real prose quality, and should suit the Bedrock tier much better.

---

## Session: 2026-09-13 19:20
- **Branch:** main
- **Files changed:** .kiro/specs/mvp1-inventory-visibility/tasks.md, CLAUDE.md, DEVLOG.md, backend/src/db/seed.js, frontend/src/App.jsx, frontend/src/api/inventory.js, frontend/src/components/Sidebar.jsx, frontend/src/index.css
- **New files:** frontend/src/pages/Launcher.jsx, frontend/src/warehouse/{Handheld,Login,Inbound}.jsx
- **Notes:** TASK-47. Built the launcher and the Goods In handheld flow while Stan was away. He corrected my proposal of a dark high contrast device theme: light is the default and the priority in this project, so the device character comes from scale and focus rather than from inverting the palette. He also asked for that preference to be durable, so CLAUDE.md now carries an onboarding section for future agents covering design preferences, the writing style rule, and the five quirks that have actually caused bugs here (inline styles beating media queries, Number(null) being a finite zero, derived values computed twice disagreeing, verifying against a mock of the cascade, and verifiers that only look for bad things). One real React bug on the way: the PIN submit effect had `busy` in its deps and set it inside, so the effect re-ran and its cleanup cancelled the request it had just started.

---

## Session: 2026-09-13 19:55
- **Branch:** main
- **Files changed:** .kiro/specs/mvp1-inventory-visibility/tasks.md, CLAUDE.md, DEVLOG.md, frontend/src/App.jsx, frontend/src/components/Sidebar.jsx
- **New files:** frontend/src/pages/Launchpad.jsx (renamed from Launcher.jsx)
- **Notes:** TASK-48. Stan asked for a name for the three card screen so he could refer to it precisely, and for the cards to stop being wordy enough to force scrolling on a phone. Named it the Launchpad, after SAP Fiori and macOS, and recorded it in CLAUDE.md with the other names. Cards lost their body text to a tap-to-expand disclosure, which is a button rather than a HoverHint because HoverHint binds mouseenter and focus only and would have hidden the text on exactly the small screens the change is for. Also gave the Control Tower a visible way out: the logo already linked to the Launchpad, but a logo that navigates is a convention rather than a signpost.

---

## Session: 2026-09-13 20:30
- **Branch:** main
- **Files changed:** .kiro/specs/mvp1-inventory-visibility/tasks.md, DEVLOG.md, frontend/src/components/Sidebar.jsx, frontend/src/index.css
- **New files:** frontend/src/components/SettingsMenu.jsx (AiModeSwitch.jsx deleted)
- **Notes:** TASK-49. Stan asked to declutter the sidebar with reference to well-designed products, and to stop the narrow-screen bar lumping unrelated buttons into one island. The sidebar had twelve interactive elements of which eight were settings; Linear, Notion, Stripe and Vercel all collapse settings behind a single entry and keep the sidebar for navigation, so that is what this does. The trigger still shows the state ("Local model · Light") because seeing which engine is answering was the original reason the tier lived in the sidebar. On narrow screens the one pill became two islands plus a bottom tab bar with labels; the pill had put the Launchpad grid icon immediately beside the Dashboard grid icon, which was my own doing last session. The narrow layout is still unverified: the resize tool has reported success while leaving the viewport unchanged all session.

---

## Session: 2026-09-13 20:45
- **Branch:** main
- **Files changed:** .kiro/specs/mvp1-inventory-visibility/tasks.md, DEVLOG.md, frontend/src/components/Sidebar.jsx, frontend/src/index.css
- **New files:** none
- **Notes:** Moved the narrow-screen navigation from a bottom tab bar back to the top, on Stan's correction. My reasoning had been thumb reach, which is sound for a native app someone installs and wrong for a desktop web app someone opens: users of a web page look up for navigation. The valuable half of the bottom bar idea was giving navigation a row of its own, since that is what actually bought the room for labels, so the top strip is now two rows: Launchpad and settings at opposite edges on the first, navigation alone on the second. Same separation of the three unrelated jobs, same labels, conventional placement.

---

## Session: 2026-09-13 21:05
- **Branch:** main
- **Files changed:** .kiro/specs/mvp1-inventory-visibility/tasks.md, CLAUDE.md, DEVLOG.md, frontend/src/App.jsx, frontend/src/components/Sidebar.jsx, frontend/src/components/SettingsMenu.jsx, frontend/src/index.css
- **New files:** frontend/src/pages/Home.jsx (renamed from Launchpad.jsx)
- **Notes:** TASK-50. Renamed the Launchpad to Home at Stan's request, and collapsed the narrow-screen settings trigger to a gear so the two row strip could become one. The label arithmetic is the constraint worth remembering: at 420px there is about 65px per tab and "Dashboard" needs 84, so labels are hidden below 560px and shown above, which covers the narrow desktop window this is actually used in. Also caught myself repeating a mistake from earlier in the session, a // comment inside a JSX opening tag, and moved it out even though the build happened to accept it.

---

## Session: 2026-09-13 21:35
- **Branch:** main
- **Files changed:** .kiro/specs/mvp1-inventory-visibility/tasks.md, CLAUDE.md, DEVLOG.md, frontend/src/components/Sidebar.jsx, frontend/src/components/SettingsMenu.jsx, frontend/src/index.css
- **New files:** none
- **Notes:** TASK-51. Gave the top bar and the settings popover the liquid glass treatment Stan tuned for the hint panel, factored into a shared .glass-surface rather than approximated twice. Two departures from his tokens, both about legibility over a scrolling backdrop rather than a momentary one: heavier blur on the bar, and a lighter drop shadow since 60px on a persistent strip reads as heavy. Home finally got a house icon, which is the other half of the grid-icon confusion he spotted earlier. Verified by temporarily widening the breakpoint so the narrow bar rendered at desktop width, then diffing the file back to byte-identical; that verifies the glass but not the narrow fit. Also caught myself putting a // comment inside a JSX opening tag for the third time this session and added it to the CLAUDE.md quirks list.

---

## Session: 2026-09-13 21:55
- **Branch:** main
- **Files changed:** .kiro/specs/mvp1-inventory-visibility/tasks.md, CLAUDE.md, DEVLOG.md, frontend/src/components/SettingsMenu.jsx
- **New files:** none
- **Notes:** TASK-52. Stan reported the settings popover as far too transparent. It was not a blur value problem: backdrop-filter makes an element a backdrop root, the popover was nested inside the bar's own .glass-surface, and so its backdrop-filter sampled an empty root and silently did nothing. Only the gradient was rendering. HoverHint portals to document.body, which is the only reason the hint panel he was comparing against frosts correctly. Portaled the popover the same way, positioned fixed against the trigger rect with scroll and resize listeners, extended the outside-click test to cover the portaled panel, and set blur to --hint-blur so it matches the values he tuned rather than approximating them. Added to the CLAUDE.md quirks list because it presents as a styling bug and is a DOM one.

## Session: 2026-09-13 23:20
- **Branch:** main
- **Files changed:** CLAUDE.md, DEVLOG.md, frontend/src/context/ThemeContext.jsx, frontend/src/components/SettingsMenu.jsx, frontend/src/components/StockPositionBar.jsx
- **New files:** none
- **Notes:** TASK-53. Two changes. First, the settings popover copy: the note read "Which engine answers Why? on an alert", which used house jargon, and never said the thing most at risk of being misread. Stan drafted the replacement and it now reads "Who writes the Why? explanations in the Alerts tab. The figures are the same in all 3 options, only the wording changes." He asked whether the section should be titled ENGINES; argued against it, because backend/src/engines is the deterministic maths and that title would tell a reader that switching a tier changes the numbers, which is the exact opposite of what slots.js guarantees. Second, theme gained Auto and made it the default: auto follows prefers-color-scheme with a live listener so a Mac following sunset updates without a reload, and falls back to light wherever the device cannot say. Stored under a new key, theme.v2, because earlier versions wrote "light" on every mount even for people who never opened the menu, so an old stored "light" could not be read as a choice. The context now returns both theme (the preference) and resolved (the appearance); StockPositionBar branched on theme === "light" and would have painted dark shading on a light page under Auto, so it reads resolved now. Both are in the CLAUDE.md quirks list.

## Session: 2026-09-13 23:55
- **Branch:** main
- **Files changed:** DEVLOG.md, frontend/src/index.css, frontend/src/pages/Dashboard.jsx
- **New files:** none
- **Notes:** TASK-54. Stan reported text too small in places and asked for a tool to tune the scale himself, which was built and published as an artifact (specimens of eight real components bound live to the tokens, presets, a master multiplier, CSS export). The audit behind it found the actual problem: index.css already declared a seven step scale with a comment calling 12px the absolute floor, and 177 inline fontSize numbers bypassed it, 30 of them below that floor, down to a 9px badge. Stan picked the large print preset. Applied it, but at six steps rather than seven: --text-md sat 1px from --text-base and named the same job ("emphasis"), which is font-weight's work, and the usage counts confirmed nobody could apply it (--text-md twice in the codebase, --text-lg never). Its two usages moved to --text-base, and Dashboard card titles moved to --text-lg so that step finally has a purpose. The tuner's own "uneven ratios" warning was wrong and was rewritten: it demanded a constant ratio, which Tailwind and Material would both fail, and which pushes the body size around to satisfy a number. It now tests the two things that actually go wrong, a pair too close to tell apart and ratios that dip rather than climb. Migrating the 177 inline sizes onto the tokens is still outstanding.

## Session: 2026-09-14 00:30
- **Branch:** main
- **Files changed:** CLAUDE.md, DEVLOG.md
- **New files:** frontend/tuners/README.md, frontend/tuners/type-scale.html, frontend/tuners/glass-tooltip.html
- **Notes:** TASK-55. Stan asked for two standing things. First, that future work follows the type scale rather than picking sizes per element, so CLAUDE.md now carries the six steps as a table with the rule stated plainly: write var(--text-sm), never a number, and if a size feels wrong reach for the neighbouring step or a different font-weight, because 14px is not a size this app has. It also says the roughly 150 existing inline literals make "match the surrounding code" the wrong instinct here. Second, that the tuners be kept together, so both now live in frontend/tuners/ with a README covering how to use them and why they are written as fragments with no doctype (one file works as both a local file and a published artifact, so the two copies cannot drift). Serving the glass tuner locally immediately exposed a bug the artifact wrapper had been hiding: a literal degree sign rendered as mojibake without a charset meta, and the code comment beside it claimed to be using an escape while using the literal. It uses an actual escape now. CLAUDE.md also tells future agents to offer a tuner when a request is really a number.

## Session: 2026-09-14 01:10
- **Branch:** main
- **Files changed:** DEVLOG.md, 15 files under frontend/src (all pages, warehouse screens and shared components)
- **New files:** none
- **Notes:** TASK-56. Migrated every inline fontSize literal onto the six step scale, 170 of them across 15 files, leaving 7 recharts axis and series labels as literals under the CLAUDE.md carve-out. Done with a codemod rather than by hand so the mapping is one reviewable table instead of 170 judgement calls. Two rules that are not rounding: chart labels are skipped, and everything 17px and up was mapped by role from an inspection pass, not by nearest number, so all four page titles are --text-xl whether they were 24 or 26. The sidebar alert badges had fixed 14px and 16px boxes sized around 9px and 10px text and needed growing to 18 and 20, which is the general hazard here: a pill sized around its old glyph clips when the glyph grows. Verified Alerts, Inventory, Activity, Home and Dashboard in the browser. Only cosmetic consequence found is the Inventory "Coverage vs Lead Time" column header now wrapping to two lines.

## Session: 2026-09-14 01:45
- **Branch:** main
- **Files changed:** CLAUDE.md, DEVLOG.md, frontend/src/index.css, frontend/src/pages/Dashboard.jsx
- **New files:** none
- **Notes:** TASK-57. Dashboard restructured after a design conversation with Stan. The top KPI card is now called Key Metrics, his name, recorded in CLAUDE.md because the card carries no visible heading and needed one to be pointed at without ambiguity. Needs Attention moves to row two at full width, on the grounds that it is the only section on the page carrying actions rather than analysis, and that at half width its Action column was wrapping to three lines on a 1546px display. Cover vs Lead + Safety and Inventory Health pair below it, then Value x Movement full width, which the matrix needed more than the pairing did. Stan asked about collapsing Needs Attention by default with a count as the lure; argued against it and he agreed: a count is strictly less information than the rows it hides, and dense-tool operators resent a dashboard that hides the answer behind a click. Built the partial reveal instead, five rows always visible with a "Show N more" button that names what is behind it, and moved the notification count into the section header as a badge tinted by the worst severity present. The badge stays visible when the section is collapsed, which is the case that would otherwise be silent. The reveal state is not persisted, unlike the collapse state: expanding to see two more rows is momentary, and restoring it next visit would undo the short page it exists to protect.

## Session: 2026-09-14 02:10
- **Branch:** main
- **Files changed:** DEVLOG.md, frontend/src/pages/Dashboard.jsx
- **New files:** none
- **Notes:** TASK-58. Three refinements Stan asked for on the restructured Dashboard. Key Metrics now carries a visible heading, matching the Section title treatment so it reads as a peer of the other cards; I had argued it did not need one, being the first thing on the page, but a name findable only in a source comment is not a shared name. The partial reveal drops to three rows read in full plus a fourth rendered under a gradient fade, which is the honest version of a scroll cue: a hard cut at row three implies the list ends there. The hidden count is measured against the three FULL rows, not the four rendered, so the teased row is not counted as read. The reveal button is now large and centred under the fade rather than a small link tucked left. First version put the fade and the button in one relative container, so bottom: 0 sat below the button and the fade washed out the control it exists to advertise; the fade is anchored to a wrapper around the table alone now. Finally, setting a filter scrolls Needs Attention back into view, which was the outstanding cost of moving the table above the charts that filter it. Only on set, never on clear, since clearing happens at the table's own chip where the user is already looking. Honours prefers-reduced-motion.

## Session: 2026-09-14 02:35
- **Branch:** main
- **Files changed:** DEVLOG.md, frontend/src/pages/Dashboard.jsx
- **New files:** none
- **Notes:** TASK-59. The Needs Attention reveal button now floats on the faded fourth row instead of sitting below the table, which is what Stan asked for and does keep the card compact: the button was adding its own height plus a gap to a section whose whole point is to stay short, while the faded row is dead space already. Extracted RevealButton so the two placements share one component; floating gets position absolute plus a shadow so it reads as above the row rather than as another faded table element, and the expanded "Show fewer" stays in the flow because there is no fade left to sit on. Also confirmed for Stan that the Dashboard and Inventory tabs share one source: both GET /api/skus and GET /api/dashboard/stats call getAnalytics() -> buildAnalytics(getDb()) over the same SELECT * FROM skus WHERE active = 1, so there is no separate dashboard store to keep in sync. Noted the two caveats that matter for his planned bulk export/edit/import: the Dashboard fetches once on mount so it needs a refetch after an upload, and PRIOR in Dashboard.jsx is a hardcoded baseline that a data import would not move.

## Session: 2026-09-14 03:20
- **Branch:** main
- **Files changed:** DEVLOG.md, backend/src/index.js, backend/src/routes/inventory.js, frontend/src/api/inventory.js, frontend/src/pages/Inventory.jsx
- **New files:** backend/src/db/csv.js, frontend/src/components/BulkEdit.jsx, frontend/src/components/Modal.jsx
- **Notes:** TASK-60. Bulk edit: export every SKU to CSV, fix it in a spreadsheet, upload it back. GET /api/skus/export emits the editable columns plus four computed context columns that are ignored on the way back in; POST /api/skus/import takes { csv, apply } and returns the same result shape either way, so the preview is literally the apply path with the write skipped rather than a second implementation that could disagree with it. sku_id is the key and is never updated: import cannot create or delete SKUs, because a typo in a key column should not silently remove a product line. Validation runs over the whole file before any write and the writes run in one transaction, so a file with a single bad row changes nothing; tested with a negative number, an unknown SKU and a duplicate key, all three reported with line numbers and applied: false despite apply: true. CSV is hand rolled in db/csv.js rather than adding a dependency, with RFC 4180 quoting, because splitting on commas turns a supplier called "Supplier ABC, Thailand" into a row where every later column lands in the wrong field, silently. Numeric comparison uses a 1e-9 tolerance, without which a spreadsheet round trip reports untouched rows as changed. One "Bulk edit" button in the Inventory header, outlined not filled, opening a two item menu; Add SKU stays the only primary action there. The import is three steps on purpose, choose then review then apply. Modal and ModalBtn were extracted from Inventory.jsx into components/Modal.jsx so the review dialog reuses the shell rather than growing a second one. Imports write one SKU_UPDATED audit event each, tagged source: csv_import, so a bulk change is not a blind spot in the Activity tab. Reseeded after testing.

## Session: 2026-09-14 03:55
- **Branch:** main
- **Files changed:** DEVLOG.md, frontend/tuners/README.md
- **New files:** frontend/tuners/table-density.html
- **Notes:** TASK-61. Stan flagged too much space between the last row and the button in the expanded Needs Attention card, and asked for a tuner rather than a guess, plus the ability to try two rows with the third faded. Third tuner built: eleven dials covering rows shown, the faded row on/off, fade height, row padding, card padding, header gap, and the button's height, width, min width, bottom offset and expanded gap. Collapsed and expanded are both previewable, since his complaint is about the expanded state and the fade only exists in the collapsed one. Presets include "Two rows", which is the layout he is considering. The specimen uses the real seeded rows rather than filler, because uniform filler wraps predictably and flatters every spacing choice. The measurement panel reads the rendered geometry rather than predicting it, since row height depends on how the Action text wraps at the current width, and it calls out the two failure modes by name: a fade taller than the row reaches into the row above, and a button taller than the row it sits on overhangs the table edge. Hit the mojibake bug a second time, this time on the middle dots in the row data, so the README now carries the rule: escape every non-ASCII character in these files, because they have no charset meta of their own and only the Artifact wrapper supplies one.

## Session: 2026-09-14 04:15
- **Branch:** main
- **Files changed:** DEVLOG.md, frontend/src/pages/Dashboard.jsx
- **New files:** none
- **Notes:** TASK-62. Applied the density values Stan tuned and pasted back. Two rows read in full with a third under a 132px fade, 9px cell padding, 22px card padding, the floating button flush at the bottom of its row, and the expanded gap down from 12px to 3px, which was the complaint that started this. Kept as a single NA_DENSITY object using the tuner's own key names, so a future export pastes straight in rather than being hunted through inline styles; NEEDS_ATTENTION_PREVIEW and the cell padding strings are derived from it. Card padding needed a scoped override rather than a token change: --space-5 is shared by every card, so moving it would have moved Cover vs Lead, Inventory Health and Value x Movement too. Section gained an optional pad prop that only this card passes. Worth noting it leaves Needs Attention 2px tighter than its neighbours, which is invisible in isolation and only reachable by measuring; reverting is deleting one prop.

## Session: 2026-09-14 04:45
- **Branch:** main
- **Files changed:** DEVLOG.md, frontend/src/components/StatCard.jsx, frontend/src/pages/Dashboard.jsx
- **New files:** none
- **Notes:** TASK-63. Compliance Position moved out of its own disclosure and into the Service & Availability row as the fourth column, which Stan proposed and which holds up on the merits: the question it answers is whether we hold enough to meet the mandate, an availability question, and the row already had an empty fourth cell that read as something missing. Removes a control and a piece of state as a side effect. It does mean the "illustrative, pending governance approval" caveat is now always on screen rather than behind a deliberate expand, so the sub-line keeps "illustrative" while dropping the longer clause for width. Second, StatCard now renders a dot for every status including ok, in green. Previously ok rendered nothing, which made absence ambiguous: a reader could not tell whether an undotted metric was healthy or simply not assessed. The original reason for the dot, that colouring seven loud numbers destroyed the focal point, still holds and is not undermined, because green reads as background where red does not. The hero Total Inventory Value stays undotted deliberately, being the one figure with no good or bad direction. Pending Stan's decision: a 6/12 month history toggle on the hero, which the data cannot currently support honestly.

## Session: 2026-09-14 05:00
- **Branch:** main
- **Files changed:** DEVLOG.md, backend/src/db/seed.js
- **New files:** none
- **Notes:** TASK-64. Warehouse operators renamed to the actual team: Wenjin (receiving, 1234), Taw (dispatch, 2345), CY (both, 3456), Stan (both, 6767). Four rather than three, and Stan is "both" so a demo never has to pick somebody else's identity to get past the login. Reseeded and verified on the Goods In PIN screen. Still parked, awaiting Stan's decision: the Total Inventory Value history toggle. The data cannot support it honestly right now, sales run 2026-03-17 to 2026-09-11 which is six months not twelve, there is no receipt history and no inventory snapshots, so a reconstructed curve would assume zero goods ever arrived and slope smoothly upward into the past, looking entirely plausible and being wrong.

## Session: 2026-09-14 05:30
- **Branch:** main
- **Files changed:** DEVLOG.md, backend/src/routes/inventory.js, frontend/src/api/inventory.js, frontend/src/pages/Dashboard.jsx
- **New files:** none
- **Notes:** TASK-65. The hero chart Stan asked for, built on data that actually exists. GET /api/dashboard/history returns monthly consumption at cost from sales_transactions, with a partial flag per month and an average computed over complete months only. The chart toggles Baseline (the existing two-bar comparison) against 6 months (bars plus a dashed average line). What it deliberately is NOT is a history of Total Inventory Value: nothing records what stock was worth on a past date, and reconstructing it backwards from sales would have to assume no goods were ever received, drawing a smooth line rising into the past that is completely plausible and completely wrong. Twelve months is not offered because the data only spans 2026-03-17 to 2026-09-11. The partial flag is the load-bearing part: March opens mid-month and September is still running, so each holds about half a month of sales, and drawn as plain bars beside a full month they read as demand collapsing, a false story told with true numbers. They are hatched instead, the same encoding StockPositionBar uses for idle stock, and the average line excludes them. The server decides partial rather than the chart, because only the server knows the real coverage window. History is fetched on first switch rather than on mount, since most visits never open it. Separately, confirmed the seed IS deterministic, two consecutive runs both produced totalInventoryValue 3711450; an earlier 3.91M reading during this session came from mutated state, not from the seed, and reseeding restores 3.71M.

## Session: 2026-09-14 05:55
- **Branch:** main
- **Files changed:** DEVLOG.md, frontend/src/pages/Dashboard.jsx
- **New files:** none
- **Notes:** TASK-66. Stan asked whether the hero chart should move under the Total Inventory Value header. Measured first: the card is 1605px wide and its content ended at 1135px, so 46% of it was empty, and the chart block was 167px tall against a 69px number block. Stacking would have moved that dead space rather than removed it, and cost roughly a chart's height of vertical space directly above Needs Attention, which was deliberately promoted. So the chart stays in the row and the frame follows the mode instead. Six months takes the free width; baseline stays compact at 248px and tight against the number, because it is a comparison of two numbers and stretching it put its two bars at opposite ends of the card, which is exactly the disconnection the original comment on this row warned about. A first attempt stretched both and proved that in one screenshot. Height is fixed at 122px across both views: height is what reflows the page below, so holding it constant stops the toggle reading as the page rearranging itself, while a width change inside a deliberate mode switch reads as the chart changing. maxBarSize caps both so neither view turns its bars into slabs.

## Session: 2026-09-14 06:10
- **Branch:** main
- **Files changed:** DEVLOG.md, frontend/src/pages/Dashboard.jsx
- **New files:** none
- **Notes:** TASK-67. Stan's two suggestions for the hero, both applied and both right. The baseline delta moves onto its own line under the figure: sitting baseline-aligned beside a 40px number it was a 17px tail, so "SGD $3.71M" and "$151K" read as one line holding two money figures of very different meaning. Stacked, it reads as a caption to the number. The row also changes from align-items: flex-end to center. Bottom alignment was correct when the chart was a 92px sparkline of similar visual weight, but the chart now carries a toggle above and a caption below, so its bars sit in the middle of a 167px block and bottom alignment lined the number up with the caption rather than with the data. Centring puts the number level with the bars. Side benefit worth noting: the value block is narrower now that the delta is not extending it sideways, so the chart sits closer to the number it explains, which is the proximity the original note on this row was trying to protect.

## Session: 2026-09-14 06:30
- **Branch:** main
- **Files changed:** DEVLOG.md, frontend/src/components/Sidebar.jsx, frontend/src/pages/Home.jsx
- **New files:** frontend/src/components/EventCredit.jsx
- **Notes:** TASK-68. "Team Puenzhiyyy" added under the event line on Home and in the Control Tower sidebar. Extracted as one EventCredit component rather than typed into both files, since a team name that lives in two places and gets edited in one is how a demo ends up crediting two slightly different teams; EVENT and TEAM are exported constants for the same reason. The team line is the stronger of the two despite sitting second: two lines of identical muted text read as one grey block nobody looks at, and of the two facts the event is context a judge already has while the team is what they are meant to remember. Weight carries that rather than size, so the block stays the height of the single line it replaced. Spelling checked against the git remote, github.com/dunstancsr-web/puenzhiyyy, three y's; Stan typed two in the request and also typed NTU-ISS where every existing string says NUS-ISS, so both were taken from the repo rather than the message and flagged back to him.

## Session: 2026-09-14 07:10
- **Branch:** main
- **Files changed:** DEVLOG.md, frontend/tuners/README.md
- **New files:** frontend/tuners/brand.html, frontend/tuners/src/{logo-template.html,build-logo.py,mz-sub.woff2,mz.b64}
- **Notes:** TASK-69. Named the fictional SME and drew it a mark. Puenzhi decomposes in Dzongkha into pun (sibling, kin) and zhi (four), so it means the four siblings rather than the four friends, and that distinction drove the Chinese choice: peng you is too weak a register. Recommended 四海米行, Sihai Mihang, Four Seas Rice Trading. 四海 comes from the Analects, 四海之内皆兄弟也, within the four seas all men are brothers, so two characters carry both halves of Puenzhi at once, the number and the kinship; it is also the register an overseas-Chinese trading house actually uses and it implies importing, which this business does. Alternatives presented were 四友 (literal but boutique) and 四合 (warm but reads invented). The suffix 米行 was chosen over 米業 and 貿易 as what a family rice business in Singapore actually calls itself. Logo sheet covers a primary lockup, a 2x2 seal, an ink version, a reversed version, scale from 16 to 64px, and an in-sidebar preview. The mark carries 米 alone because one character survives 16px and four do not. Typeface is Ma Shan Zheng under the OFL, subset from 5.86 MB to 3.7 KB and inlined, so the logo has no CDN dependency. brand.html is generated: the build script also escapes every non-ASCII character as an HTML entity, which is the fix for the mojibake bug that has now hit three of these pages. A 2x2 CSS grid bug was caught in the browser, declaring only columns with auto-flow column gave one row of four and the seal read backwards. Not yet integrated into the app; the sheet argues for the rice company as the TENANT beside StockSense rather than replacing its branding, since swapping the mark would claim the tool belongs to the customer.

## Session: 2026-09-14 07:35
- **Branch:** main
- **Files changed:** CLAUDE.md, DEVLOG.md, SUBMISSION.md, frontend/tuners/brand.html, frontend/tuners/src/logo-template.html
- **New files:** none
- **Notes:** TASK-70. The spoken pitch now lives on the brand sheet rather than only in chat: a fifteen second version written to be said rather than read, a one sentence fallback, the note that the load-bearing line is "it keeps both halves of ours: the four, and the family", and three likely judge questions with answers, including the caution not to over-claim the Dzongkha. Referenced from SUBMISSION.md, which is where anyone preparing the submission actually looks, and from CLAUDE.md so a future agent finds it. Also added three sidebar placement options, each a pattern a shipping product uses: A workspace header (Slack, Notion, Linear) with the company owning the header and StockSense demoted to a footer line; B product header plus an account chip (Stripe, AWS, Salesforce); C co-brand lockup (SAP, ServiceNow). Recommended B, on the grounds that A is the better product decision and the worse demo decision, since the judges are evaluating StockSense and burying it to win realism trades the wrong thing, while C is cramped at 232px and implies a partnership that does not exist. Fixed a stale line in CLAUDE.md that still said Key Metrics has no visible heading.

## Session: 2026-09-14 08:00
- **Branch:** main
- **Files changed:** DEVLOG.md, frontend/src/index.css, frontend/src/components/Sidebar.jsx, frontend/tuners/brand.html, frontend/tuners/src/logo-template.html
- **New files:** frontend/src/components/Tenant.jsx, frontend/public/fonts/brush-subset.woff2
- **Notes:** TASK-71. Client mark integrated: option B's structure (StockSense keeps the header, the company is the account below it) carrying option A's two-line wordmark with the English translation beneath. Two departures from the mockup, both deliberate. The chevron is gone: it borrowed the account-switcher look and a chevron promises a menu, which with one account would be empty, so the chip keeps the shape and drops the promise. And it is designed against the LIGHT sidebar rather than the sheet's slate preview, since light is the default and what gets demoed, so the seal is cinnabar on white; verified in dark too, where --seal lightens to #d6453a so the mark keeps its weight on slate. The brush font is self-hosted at public/fonts/brush-subset.woff2, 3.7 KB, with font-display: block rather than swap: the file is tiny and same-origin so the block period is imperceptible, while a brand mark flashing a fallback face reads as broken in a way body text does not. The brand sheet's option B panel was updated to match what shipped rather than left showing the proposal.

## Session: 2026-09-14 08:25
- **Branch:** main
- **Files changed:** DEVLOG.md, frontend/tuners/brand.html, frontend/tuners/src/logo-template.html
- **New files:** none
- **Notes:** TASK-72. Two studies added to the brand sheet, nothing shipped to the app yet pending Stan's pick. First, Stan asked about using the full 2x2 seal in the sidebar with the wordmark shortened to 四海米. Rendered all four combinations at the size the sidebar actually uses, 30px in a 214px column, which is the only size that settles it: four characters in a 30px square gives each glyph about 13px and the seal reads as texture rather than as characters. 四海米 is legitimate Chinese and the logic is sound, the chop carries the registered name and everyday use drops 行, but the problem is positional: beside the full seal the wordmark says less than the mark next to it, and beside the 米 seal the character appears twice in 40 pixels. The full seal stays the right mark for print and slides at 88px. Second, a study of the StockSense tile, which disagrees with the seal on three counts: the seal is a solid stamp with a filled glyph and deep pigment, the tile is a 2.2px hairline drawing in a bright SaaS blue, so one looks pressed into the page and the other sketched on it. Four options drawn; recommended C, solid bars with the trend line over them in #1d4ed8. The colour change matters more than the drawing, since #3b82f6 is brighter than #b3271e is deep, so the junior mark currently wins the first glance. A rice-grain mark was drawn and rejected on the grounds that borrowing the client's subject for the product mark is exactly what the tenant arrangement exists to avoid.

## Session: 2026-09-14 08:55
- **Branch:** main
- **Files changed:** DEVLOG.md, frontend/src/components/Sidebar.jsx, frontend/src/components/Tenant.jsx, frontend/src/pages/Home.jsx, frontend/tuners/brand.html, frontend/tuners/src/logo-template.html
- **New files:** frontend/src/components/AppMark.jsx
- **Notes:** TASK-73. Shipped StockSense mark option C: solid bars with the rising line over them, in #1d4ed8. Extracted as AppMark so the sidebar and Home stop drawing the same tile twice at two sizes and stroke weights. Fixed the affordance Stan flagged on the tenant row: fill plus border plus radius is the universal press-me signal, and the row had all three, so a thing that does nothing looked like the most clickable item in the sidebar. Removed the box entirely and replaced it with an ACCOUNT label over the value, which is the shape of a read-only field, plus a hairline above to group it with the header. Useful side effect: the Home link below it is now the only box in that column, so the one genuinely interactive element reads as interactive. Mark heights now match the text block each sits beside, measured rather than guessed: 56px on Home against a 33px title plus 23px tagline, 45px in the sidebar. Also added four Home placement options for the client mark to the brand sheet, recommending the signed-off footer: it is the one placement where the full 2x2 seal is both legible at 52px and idiomatic, since a chop is pressed at the end of a document rather than printed in its letterhead, and it takes nothing from StockSense at the top. Caught a CSS class collision while building it, .tag was already the naming table's "the pick" badge and was painting every Home tagline cinnabar.

## Session: 2026-09-14 09:15
- **Branch:** main
- **Files changed:** DEVLOG.md, frontend/src/components/Tenant.jsx, frontend/src/pages/Home.jsx
- **New files:** none
- **Notes:** TASK-74. Home rebuilt as option 4, client-led: the full 2x2 chop at 56px with 四海米行 and FOUR SEAS RICE TRADING as the page identity, and StockSense signing the foot as "Running on StockSense" above the team credit. Home is the only surface with room for the full seal; at the sidebar's 30px the same mark is texture. The trade is real and Stan made it knowingly: the product being judged is no longer the largest thing on the first screen, mitigated by the footer and by the Control Tower sidebar, which still leads with StockSense. Copy rewritten: "Stock arrives, stock leaves, and somebody has to decide what to do about what is left" read as a mission statement, which is the wrong job for the top of a portal since it described the domain rather than asking for a decision. Replaced with a question and a count, "Where are you working today?" over "Three workspaces, one inventory. Pick one to begin.", which tells the reader what the screen wants and that there are exactly three answers. Added a direction arrow to each live card: a card with a title and a paragraph is a description, the same card with a direction cue is an invitation. Goods Out deliberately has no arrow, since it goes nowhere and an arrow would promise that it does.

## Session: 2026-09-14 09:45
- **Branch:** main
- **Files changed:** DEVLOG.md, frontend/tuners/README.md
- **New files:** frontend/tuners/home-layouts.html, frontend/tuners/src/{home-template.html,build-page.py}
- **Notes:** TASK-75. Stan asked for a design critique of Home and then for mockups. Measured before critiquing rather than describing impressions: 214px of a 929px viewport passes before the first pixel of content, because align-items centre on a 100vh container centres a short page in a tall viewport. Other findings: two alignment systems on one page, masthead left and footer centred; the arrow anchored to the card's muted device label rather than its title; no hover state at all, so at rest and on hover the card is pixel-identical; "What is this?" three times as a button nested in a link; and the disabled card dimmed wholesale at 0.6 opacity, which fades the NEXT UP badge along with everything else so it reads as broken rather than deliberate. Four mockups built, all drawn in an identical fixed 560px frame because a mockup that grows to fit its content cannot show a vertical placement problem, with the current state included and the dead space hatched and measured. A corrected, B centred, C split with the workspaces as full-width rows, D split plus a desktop-aware primary. Recommended C: the empty width is used rather than centred away, and a row with an icon, a label and an arrow at the far edge is the shape of a list item you tap where a square tile is the shape of a thing you read. Flagged the real cost of D, which is that Goods In is the flow that is actually built and demoed, so demoting it to make a UX point puts the finished feature second. Added build-page.py, the generic form of build-logo.py.

## Session: 2026-09-14 10:15
- **Branch:** main
- **Files changed:** DEVLOG.md, frontend/tuners/README.md
- **New files:** frontend/tuners/home-editor.html, frontend/tuners/src/editor-template.html
- **Notes:** TASK-76. Stan picked option C and asked for a PowerPoint-style drag and drop editor so he can rearrange it himself and then argue with me about the result. Built, with one deliberate constraint: it snaps to ZONES AND ORDER, never to pixels. Free positioning would let a layout be designed here that cannot be built responsively, so the editor offers exactly what a CSS grid with flex columns can express, which means anything arranged in it can actually ship. Three zones, full width plus two columns, nine draggable blocks (seal, wordmark, question, sub-line, the three workspaces, the StockSense credit and the team credit), sliders for column split, gaps, top and side space, seal and wordmark size, and toggles for tiles versus rows and centred text. Insertion point is computed against the midpoint of each existing block so the gap opens where the cursor is rather than always appending. The export is a HOME_LAYOUT object I can apply directly. It also argues back live: a critique panel under the canvas objects as he arranges, catching the workspaces being split across zones, the sub-line ordered before the question it explains, StockSense dropped entirely, top space small enough to crowd or large enough to bring the float back, and centred text inside full-width rows. Verified a real drag by dispatching the event sequence: block moved zones, export updated, correct objection fired.

## Session: 2026-09-14 10:45
- **Branch:** main
- **Files changed:** DEVLOG.md, frontend/tuners/README.md
- **New files:** frontend/tuners/home-centred.html, frontend/tuners/src/centred-template.html
- **Notes:** TASK-77. Stan switched to option B and asked for D's device separation without D's split columns, plus a bigger sub-line and the help restored. Three variants built, all on one centre axis. E1 hero and pair, the usable workspace as one wide card above a labelled rule with the two handheld flows side by side beneath; E2 one row with a "For this device" badge and "Handheld" captions, least disruptive since no card is demoted; E3 a single narrow column of stacked rows. Recommended E1: the separation is carried by labelled rules rather than by a layout split, which costs one line of height and keeps the centre line unbroken all the way down. Flagged the type conflict rather than applying the request literally: the sub-line two steps up from --text-sm lands on 22px, exactly the question's size, and two lines of identical size read as a paragraph rather than as a heading and a caption. Moved both, question to 30px and sub-line to 22px, which is the full two steps while keeping two steps between them, with the one-step-each fallback documented on the sheet. Help returns as an i in each card corner opening on hover or focus, matching HoverHint's existing behaviour so it stays reachable by tap, rather than three repeated "What is this?" buttons.

## Session: 2026-09-14 11:20
- **Branch:** main
- **Files changed:** DEVLOG.md, frontend/src/components/ColHint.jsx, frontend/src/index.css, frontend/src/pages/Home.jsx
- **New files:** none
- **Notes:** TASK-78. Home shipped: E1 wide, E3 narrow, from ONE markup. The two layouts differ only in the pair's column count and the shell width, so a single breakpoint at 780px covers both; declared as .home-shell and .home-pair classes rather than inline styles, because an inline grid-template-columns beats a stylesheet rule and the breakpoint could never reach it, which has shipped as a real bug here before. Verified by loading the page in a 596px iframe and reading the computed style: the pair resolves to one 520px column, which is E3. The device grouping is carried by labelled rules, "For this device" and "On the warehouse floor", rather than by splitting the page, which is the thing Stan disliked about option D. Help is now a 12px muted ColHint beside each name, the same component every table header uses: it already stops propagation, which matters because the card is a Link, and it opens on focus as well as hover so it stays reachable by tap. ColHint's "How to read it" section became optional, since these cards have one description and an empty header over nothing is worse than no header. Also fixed the float: the page anchors with clamp(40px, 9vh, 96px) instead of centring, which was putting 214px of nothing above the first pixel of content. "Next up" became "Building in progress", and at that length the badge alongside the text squeezed "Picking and dispatch - handheld" onto three lines and unbalanced the pair, so it moved under the label, which is where a status about the whole card belongs anyway.

## Session: 2026-09-14 11:50
- **Branch:** main
- **Files changed:** DEVLOG.md, frontend/src/pages/Home.jsx
- **New files:** frontend/src/components/TowerIcon.jsx
- **Notes:** TASK-79. Drew a control tower icon rather than borrowing one, because neither lucide reference says it alone: lighthouse reads as coast and rescue, radio-tower reads as broadcast with no place anyone sits. The union is a structure with a CABIN and SIGNAL, and the cabin does the identifying: a wide glass box on a narrow stem is the silhouette of an airport tower and of nothing else, and with it present the arcs stop meaning radio and start meaning in contact with the floor. Built on lucide's grid so it sits with its neighbours, 24x24, 2px round-capped strokes, currentColor, with the beacon as the one filled element so it reads as lit. Two drafts were wrong and were fixed against the rendered result rather than the source: the first put short arcs either side of the beacon, which put two curves beside a dot and read as ears on a face, making the whole thing a chess rook; the second had the arcs too close to the cabin and slightly low, so at large sizes they read as wings attached to it. Also enlarged all three workspace glyphs, tile 52 to 62 and 40 to 50 with the glyph ratio from 0.48 to 0.56, and changed the Goods Out badge from "Building in progress" to "Coming soon", which is short enough to sit on the name row where it belongs, so the sub-line no longer wraps and the pair is balanced again.

## Session: 2026-09-14 12:20
- **Branch:** main
- **Files changed:** DEVLOG.md, frontend/src/components/EventCredit.jsx, frontend/src/pages/Home.jsx
- **New files:** frontend/src/hooks/useDeviceClass.js
- **Notes:** TASK-80. Stan asked how the platform senses the device, and the honest answer was that it did not: "For this device" was a fixed string over a hardcoded Control Tower, so opening Home on a phone told the reader that the desktop workspace was the one for their phone. Now detected. The signal is pointer: coarse, not width, because width answers how much room there is, which is the right question for layout and the wrong one here: a desktop window dragged narrow is still a desktop with a mouse, and a tablet in landscape is still a touch device at 1024px. User-agent sniffing was rejected as a stale list of strings that browsers actively lie in, answering what the software is called rather than how it is being touched. Detection changes the ORDER AND LABELS only, never what is available: both groups always render, so a wrong guess on a touchscreen laptop costs a little scrolling rather than access. On handheld the floor group leads with Goods In as the hero and the second rule reads "In the office". Testing it caught a real subtlety: matchMedia returns a NEW MediaQueryList per call, so dispatching a synthetic change event on a freshly created one never reaches the hook's listener, and the first test looked like a failure of working code; stubbing matchMedia and remounting through the router proved it properly. Also caught a single card stranded at half width in the two-column grid on handheld, so a Group component now uses the grid only when there are two cards to put in it. Finally, the event and team credit share one row on Home via an inline prop; the sidebar keeps two lines because at 230px one row would wrap mid-name.

## Session: 2026-09-14 12:50
- **Branch:** main
- **Files changed:** DEVLOG.md, frontend/src/components/Sidebar.jsx
- **New files:** none
- **Notes:** TASK-81. Sidebar brought in line with Home. The inconsistency that mattered was that the two screens told opposite stories: Home says "Four Seas Rice's system, running on StockSense" while the sidebar said "StockSense, one of whose accounts is Four Seas Rice", so the identity inverted as a user walked from one into the other. The sidebar is now client-led too, the seal and 四海米行 as the masthead and StockSense signing the foot with the same "Running on StockSense" line Home uses. The ACCOUNT label went with it, and its removal is the tell that the change is right: that label existed only to explain a company name sitting under a product name, and at the top it needs no explaining. "Control Tower" moves from being a sub-line under StockSense to naming the screen, carrying the TowerIcon that marks the same workspace on Home; an icon becomes learnable when the same mark names the door and the room behind it. Nav icons 17 to 20px so the two screens scale alike. Per Stan: the exit stays at the bottom, keeps the home icon, is renamed "All workspaces" and is now the largest control in that region, full width and taller than a nav row, because someone who opened the Control Tower by mistake should not have to hunt for the way back. Added overflowY auto to the panel: it fits a 929px viewport with room to spare, but the panel is taller than it was and a 13 inch laptop in landscape is nearer 700px, where a credit line would disappear off the bottom edge silently.

## Session: 2026-09-14 13:15
- **Branch:** main
- **Files changed:** DEVLOG.md, frontend/src/components/AppMark.jsx, frontend/src/components/Sidebar.jsx, frontend/src/index.css
- **New files:** none
- **Notes:** TASK-82. Sidebar colour restraint, on Stan's note that the panel was too distracting. It had three colours competing before a single figure was read: a purple section icon, a blue product mark and a red alert badge, and only the badge carried information. Control Tower icon is now 24px, up from 19, and --text-primary rather than purple: the tint is wayfinding on Home where three workspaces must be told apart, but here there is one workspace so the colour distinguishes nothing, and matching the title's colour makes icon and label read as a single lockup. The StockSense credit mark is muted to --text-muted and returns to brand blue on hover or focus-within, which is the right weight for an attribution rather than a control. Implemented by having AppMark read its background from a custom property with the blue as fallback, so an ancestor can mute it without an inline style winning. "All workspaces" gains a hover: background to --blue-light, border and text and icon to --blue, and the icon steps 3px toward the edge it leads to. One gesture with three properties moving together rather than a flourish; the transform is dropped under prefers-reduced-motion. All of its colour moved out of inline props into a .sidebar-exit class, because an inline background beats a stylesheet rule and a :hover could never have reached it. Also swapped the sidebar masthead from the full 2x2 chop to the single 米 at Stan's request, which matches the earlier finding: four characters in a 40px square give each glyph about 17px and read as texture, where one character holds. The full seal keeps Home at 56px.

## Session: 2026-09-14 13:40
- **Branch:** main
- **Files changed:** DEVLOG.md, frontend/src/components/Sidebar.jsx, frontend/src/index.css
- **New files:** none
- **Notes:** TASK-83. Two hover refinements. The "All workspaces" icon now scales 1.18 from centre instead of stepping 3px left: the sideways shift was the wrong gesture because the eye reads the icon and label as one unit, and growing in place keeps every other element exactly where it is. Transforms never affect layout, so the label provably cannot move; verified the icon goes 19px to 22.4px with the row's text origin unchanged. Second, the nav rows gained a hover at all, which they did not have: at rest and under the pointer they were pixel-identical, so the only feedback that a row was live was the cursor. They now take a --surface-2 grey fill with the label darkening to --text-primary. The active row deliberately keeps its blue under the pointer, because a grey wash over the selected item would read as the selection being cleared. Doing this required moving the NavLink's styling out of an inline style function into a .nav-item class with NavLink's own active class carrying state, since an inline background beats a stylesheet rule and a :hover rule could never have reached it. That is the same quirk that has now bitten this repo four times.

## Session: 2026-09-14 14:00
- **Branch:** main
- **Files changed:** DEVLOG.md, frontend/src/components/Sidebar.jsx, frontend/src/components/SettingsMenu.jsx, frontend/src/index.css
- **New files:** none
- **Notes:** TASK-84. Settings trigger gets the same grey-fill hover as the nav rows, for the same reason: it had none, so the only feedback was the cursor. Its styling moved from an inline object into .sidebar-settings, with the open state carried by a data attribute rather than an inline background, which would have beaten the hover rule; verified the popover still opens and the trigger keeps its fill while open. Control Tower section header now returns to purple on hover, the same bargain the StockSense credit makes, colour available on demand rather than spent by default. Implemented with currentColor inherited from a wrapper span rather than a stroke prop, which means one property carries the transition instead of a stroke and a fill separately, and which sidesteps the fact that a CSS custom property cannot be used inside an SVG presentation attribute. Label up one step to --text-base and the icon to 26px.

## Session: 2026-09-14 15:10
- **Branch:** main
- **Files changed:** DEVLOG.md, backend/src/db/init.js, backend/src/db/seed.js
- **New files:** none
- **Notes:** TASK-85, part 1 of the inventory history work. New inventory_history table: one row per SKU per month, LONG format, which is the decision that lets it carry any number of months since the header never changes and only the row count grows. No closing_value column, it is closing_qty * unit_cost_sgd derived in SQL, because storing it would be a third place for the same number; unit_cost_sgd IS stored per row since cost at that time is a point-in-time fact. Seed extended from 26 to 105 weeks of sales so every one of the 24 periods has real transactions behind its issues figure, and buildHistory walks BACKWARDS from the known on-hand so the chain ties exactly rather than approximately: closing of the newest period is the actual stock, issues are summed from fulfilled sales, and only receipts are constructed, clamped so opening never goes negative. A verifyHistory() tie-out runs at the end of every seed and fails loudly: every period must balance, every opening must equal the prior closing, and the newest closing must equal inventory_positions.on_hand_qty. Two PRNG bugs found and fixed by measuring rather than reasoning. The shared mulberry32 stream is threaded through every SKU in order, so drawing extra numbers anywhere shifts every later SKU: extending the sales tail moved GMROI 0.68 to 0.67 and DIO 106 to 108, and passing the shared rng into buildHistory moved them again. Both now use per-SKU streams seeded from the SKU id. Verified by diffing the last 95 days of sales against a temp DB seeded from the pre-change generator: byte-for-byte identical, and all nine headline KPIs plus health counts and exception count are unchanged. History total at the newest period is SGD 3,711,450, which equals stats.totalInventoryValue exactly, so the chart cannot disagree with the KPI strip.

## Session: 2026-09-14 15:45
- **Branch:** main
- **Files changed:** DEVLOG.md, backend/src/routes/inventory.js
- **New files:** none
- **Notes:** TASK-85, part 2. GET /dashboard/history now returns closing stock and closing value per period from inventory_history alongside the consumption it already returned, plus latest and prior so the frontend can drop the hardcoded PRIOR constants. Cap raised 24 to 36. Both issues_qty (from history) and consumed_qty_mt (from sales_transactions) are returned deliberately: they are two independent readings of the same fact, and returning both is how a disagreement becomes visible rather than silent. They currently agree for all 24 periods. Renamed value_sgd to consumed_value_sgd and qty_mt to consumed_qty_mt, since the old names would have collided with closing value. New GET /skus/history/export?months=N and POST /skus/history/import, a second file rather than more columns on the SKU export because the two have different grains; merging them would mean either repeating every SKU field 24 times or adding a column per month, and column-per-month is exactly the shape that stops working past a year. The import mirrors the existing preview-then-apply contract and adds the checks that make the dataset self-verifying: each row must balance as it would be AFTER the edit, and each period's opening must continue from the previous period's closing, checked once over the whole file since that can only be judged after every edit is known. Mismatch against today's on-hand is a warning rather than an error, because someone correcting history may legitimately fix the months before the position. Verified: a cost edit passes untouched, an unbalanced row is refused with the arithmetic quoted back, and a row that balances but breaks the chain into the next month is refused too.

## Session: 2026-09-14 16:30
- **Branch:** main
- **Files changed:** DEVLOG.md, frontend/src/api/inventory.js, frontend/src/components/BulkEdit.jsx, frontend/src/pages/Dashboard.jsx
- **New files:** none
- **Notes:** TASK-85, part 3. PRIOR is deleted: the eight hand-set constants that drove every trend arrow are gone from Dashboard.jsx and grep confirms no reference remains. Only ONE arrow survived, and the reasoning matters more than the code. Of the eight metrics, inventory value is the only one with a like-for-like historical basis: today's figure is on-hand times cost and a past month's is that month's closing times the cost recorded for it, the same measurement. Every other KPI is built from rolling 30 and 90 day velocity, from current reservations, or from today's policy thresholds, none of which are stored per month; back-computing them would compare a rolling window against a calendar month and call the difference a trend. Checked financials.js directly rather than assuming. So seven arrows were REMOVED rather than faked, and ppTrend and numTrend went with them. The way to get them back is to snapshot KPIs monthly from here on, not to reconstruct them. Hero chart now plots month-end inventory value from inventory_history with 6M, 12M and 24M window buttons; consumption moved into the tooltip rather than getting a second chart, since the month's stock level and what left the warehouse belong together. Labels are suppressed above 12 months where they would collide. BulkEdit gained Download and Upload history behind a divider, driven by a DATASETS table so the review dialog, error handling and toast are shared rather than duplicated; the picker's target dataset rides a ref because a state update would not have landed before the change handler reads it. Verified end to end over real HTTP: the newest history closing is SGD 3,711,450 and stats.totalInventoryValue is SGD 3,711,450, and a deliberately unbalanced upload is refused with the arithmetic quoted back.

## Session: 2026-09-14 19:30
- **Branch:** main
- **Files changed:** backend/src/engines/financials.js, backend/src/routes/inventory.js, frontend/src/components/ColHint.jsx, frontend/src/components/StatCard.jsx, frontend/src/index.css, frontend/src/pages/Dashboard.jsx
- **New files:** none
- **Notes:** TASK-86, logged after the fact: the code shipped in commit 0d6f83e without an entry. Key Metrics labels rewritten in plain English for the SME owner, with the industry term kept one hover away as an "Also called:" footer via a new optional `term` on ColHint. Only the six jargon labels changed (Fill Rate, Coverage in Target Band, Compliance Position, Turnover, GMROI, Excess & Obsolete); Stockout Risk and Overstock were already plain words. Mapping Stan's five risks onto the cards showed two were fused into E&O (slow moving and idle) and ageing had no card at all, so E&O's sub-line now splits slow moving and idle, and its hint points to Needs Attention for ageing. eoSlowValue and eoIdleValue partition the same eoSkus array that eoValue sums, so they add back to it by construction. Hero chart bars are now stacked into stock that arrived that month and stock carried over, under FIFO: arrived = MIN(receipts_qty, closing_qty), evaluated per SKU row INSIDE the SUM so one SKU's receipts cannot offset another's issues. carried_value_sgd is subtracted from the total rather than summed separately, so the stack equals the hero figure exactly. Windows became 6M, YTD, 12M, ALL; 24M was dropped because with 24 months stored it drew the same chart as ALL. New --blue-soft token, since --blue-light is a surface tint too pale for a bar. Verified: 0 split mismatches across 24 periods, newest total 3,711,450 equals stats.totalInventoryValue, E&O parts sum to eoValue, all nine KPIs unchanged.

## Session: 2026-09-14 21:00
- **Branch:** main
- **Files changed:** frontend/src/index.css, frontend/src/pages/Dashboard.jsx
- **New files:** none
- **Notes:** TASK-87. Needs Attention was a dead end from the health chart: clicking the green band filtered to four real SKUs, none with an exception, and the table said "No open exceptions match this filter". Fixed with a second scope rather than a special case for green, so every chart filter works the same way. New ScopeToggle in the section header (Needs action / All SKUs, with counts), reusing the hero chart's window button styling, via a new optional `actions` slot on Section. buildNoActionRows appends the remaining SKUs at priority 9, so switching scope adds to the bottom and never reshuffles the urgent rows. Each row shows the figures the health rules read (cover, movement class, available MT) rather than just the word healthy, and its value is labelled "stock value" because every other row values a problem. The tag follows the SKU's own health_status, not a fixed green, since having no exception and being GREEN are separate tests. That needed a missing --orange token pair. The badge still counts exceptions only in both scopes, so revealing healthy rows never makes the alarm number rise. The empty state now names how many SKUs the filter matched and offers "Show them anyway". Scope is not persisted, same reasoning as the reveal. Also filled the partial-month bar with fillOpacity 0.55. Verified in the browser: green band gives the new empty state, the button reveals the 4 healthy SKUs, and All SKUs with no filter lists 6 exceptions in their original order followed by 4 healthy rows.

## Session: 2026-09-14 22:30
- **Branch:** main
- **Files changed:** .gitignore, backend/src/index.js
- **New files:** none (backend/.env.example removed from the index; .git/hooks/pre-commit added locally, not tracked)
- **Notes:** TASK-88, secrets handling before going live. Found that nothing loaded backend/.env at all (no dotenv, no --env-file), so a correctly filled file would have been silently ignored. index.js now calls Node's built-in process.loadEnvFile first, above every other require, because llm/provider.js reads its settings into constants at require time. ENOENT is expected and swallowed, since a host supplies values through its own environment, and real environment values are not overwritten by the file. The gateway key was once typed into .env.example, which is tracked; it was moved to backend/.env before any commit and history was checked clean. .env.example is now gitignored and removed from the index with git rm --cached (a gitignore entry alone does nothing for an already tracked file). A local pre-commit hook blocks staged env files, env-style secrets with a literal value, 48 char hex tokens, sk-ant- keys and AKIA ids, printing file and line but never the value. Its first version flagged `const HEALTH_TOKEN = {` in Dashboard.jsx, which would have refused the next real commit; tightened to literal values only. Verified on 16 cases plus the whole repo staged as new files, with no false alarms. Hosting decision recorded: AWS Lightsail and the organizers' gateway, one AWS sponsored USD 100 pool for both, per the briefing and Slack. Noted for later: LLM_PROVIDER is read but never used, three provider.js error messages still point at the now untracked .env.example, and the daily call cap does not cover the gateway path.

## Session: 2026-09-14 23:30
- **Branch:** main
- **Files changed:** DEVLOG.md, backend/src/llm/slots.js
- **New files:** none
- **Notes:** TASK-89. First live gateway call (Sonnet 4.5, 853 in / 183 out tokens, about USD 0.005) produced "580 MT exceeds the maximum policy level of 400 MT by 220 MT" and marked it verified. Every figure was real: 580 is available stock, 220 is on-hand 620 minus the 400 maximum. The model paired the wrong two, and a reader doing the subtraction concludes the figures are invented. Slots guarantee values, not pairings, and catching a pairing after the fact would mean parsing sentences. So WITHHELD_BY_ALERT removes the slot that cannot belong: OVERSTOCK no longer offers {available_stock}, and a model reaching for it is rejected as an unknown placeholder and retried. on_hand_stock and overstock_amount descriptions now name the relationship, worded to stay true for every alert type since descriptions are shared. Verified: the exact failing template is rejected, the corrected one renders "620 MT exceeds 400 MT by 220 MT", the other five alert types still get available_stock, and a second Sonnet call wrote "holding 620 MT against a maximum policy level of 400 MT, putting it 220 MT over" on the first attempt (USD 0.0056). Found along the way, not fixed: REORDER triggers on inventory position against the SUGGESTED reorder point but is offered available_stock and the POLICY reorder point, the same class of flaw with no live REORDER alert to expose it today; bench-models.js only scores the freetext path and never loads slots.js, so it cannot measure the path judges see; the explanation cache key omits the provider, so switching to cloud can return a cached llama3 answer for up to 6 hours.

## Session: 2026-09-15 00:30
- **Branch:** main
- **Files changed:** .dockerignore, DEVLOG.md, backend/src/db/audit.js, backend/src/index.js, backend/src/llm/explain.js, backend/src/llm/provider.js, backend/src/routes/inventory.js, frontend/src/api/inventory.js, frontend/src/components/SettingsMenu.jsx, frontend/src/pages/Activity.jsx, frontend/src/pages/Alerts.jsx
- **New files:** backend/src/llm/demoAccess.js, frontend/src/lib/llmTier.js (and a PIN rule added to the local, untracked .git/hooks/pre-commit)
- **Notes:** TASK-90, guarding the shared credit before going public. The explanation tier was one module-level variable, so on a public URL one visitor choosing AWS Bedrock turned on paid calls for every visitor, and the credit is the same AWS sponsored USD 100 that pays for hosting. The tier is now PER REQUEST: each browser stores its choice (localStorage) and sends it with each Why?; POST /llm/mode was removed so an old client fails loudly. The cloud tier needs a demo PIN (DEMO_PIN, server environment only) exchanged at POST /llm/unlock for a stateless HMAC-signed pass that lasts 2 hours and lives in sessionStorage, so it dies with the tab on a shared demo laptop. Rate limits: 5 wrong tries locks a client for 15 min, and past 30 wrong tries in total unlocking pauses for everyone, since per-client limits do nothing against guesses spread across addresses. Production refuses the paid tier entirely with no PIN or one under 6 characters. app.set("trust proxy", 1) in production, because behind the Lightsail or Render load balancer every visitor would otherwise share the balancer's address and one person's wrong guesses would lock out all. The daily call cap now covers every cloud backend (it was checked inside callAnthropic only, leaving the gateway uncapped) and counts attempts, not successes. Production defaults to rules and does not offer the local tier unless OLLAMA_URL is set. The explanation cache key now leads with the tier, so a paid request cannot be served a cached llama3 answer. Removed the unused LLM_PROVIDER setting and rewrote error messages that pointed at the now untracked .env.example. Audit gains LLM_UNLOCKED and LLM_UNLOCK_LOCKED_OUT (not every wrong guess, so a guessing flood is not a row flood; the PIN is never logged), rendered on the Activity page. Also fixed: frontend request() spread options after the merged headers, so the first caller to pass any header silently lost Content-Type; .dockerignore matched .env only at the context root, so backend/.env with the real key was sent to every build (no image ever contained it, the Dockerfile never copies it) and now **/.env is excluded; the sidebar Settings popover had a 248px minWidth inside a 230px sidebar with overflow auto, clipping its right edge, which the PIN form's Unlock button made visible. Verified against a production-mode instance on :4100 with a random test PIN, a scratch database and a fake gateway address so nothing could spend credit: default rules, local not offered, unlocked requests refused and uncounted without a pass, 4th wrong PIN says 1 try left, 5th locks, a correct PIN while locked is refused, another client unaffected, pass lasts 120 min, tampered and expired and wrong-secret passes refused, a valid pass reaches the (fake) gateway and is counted, cap of 2 refuses the third before calling, 30 distributed wrong guesses pause even a correct PIN, short and missing PINs switch paid mode off, a server without OLLAMA_URL falls back from local to rules. In the browser against the built bundle: unlock flow, status line "Unlocked until ... 0 of 200 paid calls today", Why? with a pass counted once with no locked notice, a forged pass shows the locked notice and falls back to Rule-based. An Enter key that would not submit the PIN form was traced to the browser automation extension, not the app: a fresh page with only the app's own code does not cancel Enter. The dev flow is unchanged: no PIN locally, Local model default, click-again confirm for the paid tier.

## Session: 2026-09-15 01:15
- **Branch:** main
- **Files changed:** DEVLOG.md
- **New files:** .github/workflows/container.yml
- **Notes:** TASK-91, building the container without Docker on the Mac. The Mac is arm64 with no Docker, AWS CLI or lightsailctl, and Lightsail's container service runs linux/amd64. Of three routes (GitHub builds it, a plain Lightsail server with no containers, or installing Colima, Docker CLI, AWS CLI and lightsailctl locally), Stan chose GitHub Actions. The workflow builds linux/amd64 on an amd64 runner, so better-sqlite3 compiles for the real target instead of under emulation, and loads the image locally so the image that is tested is the image that is pushed. Before publishing it starts the container with a throwaway DEMO_PIN and no gateway credentials, waits for /api/health, checks the home page and the /alerts deep link return the built app, checks the database seeded to the known 3,711,450, checks the production guards (rules default, local not offered, paid tier unavailable without credentials, a paid request without a pass refused), checks it runs as the node user, then fails on any .env file anywhere in the image or any credential-shaped string in backend/src or frontend/dist, printing file names only. Publishes to ghcr.io/<owner>/<repo> tagged sha-<commit> and latest, from main only. Found while planning: the GitHub repo is private (the unauthenticated API returns 404), so Actions uses the 2,000 free private minutes and the published package starts private; Lightsail can only pull public images, so the package must be made public at deploy time, which exposes the app's code before the repo itself is shared. Verified locally what can be without Docker: YAML parses, the built index.html has the root div the test greps for, the secret scan finds nothing in the real files and does catch a planted fake key, and the dev server's seeded value matches the expected figure. The workflow itself cannot run until it is pushed.

## Session: 2026-09-15 02:00
- **Branch:** main
- **Files changed:** DEVLOG.md, SUBMISSION.md, WRITEUP.md
- **New files:** docs/(Stan) DEPLOY-LIGHTSAIL.md, backend/scripts/check-deploy.js
- **Notes:** TASK-92. The first Actions run (c5a349c) went green in 2m 16s: smoke tests and the secret scan passed, and sha-c5a349c and latest were published with the same digest, which is the evidence that the pushed image is the tested one. Wrote the deploy-day checklist from AWS's Lightsail documentation rather than memory, which surfaced three facts that changed it: the container service URL is HTTPS by default with the load balancer talking HTTP to the container (one proxy hop, matching trust proxy 1); a container service is billed until it is DELETED, not merely disabled; and scale must stay at 1, because each node would run its own SQLite database, PIN lockout counter and daily cap. check-deploy.js verifies a live deployment without a PIN or credit: HTTPS, health, built site and deep link, seeded data (warning rather than failure if stock was edited), rules default, no local tier, paid tier configured and PIN-gated, a paid request without a pass refused as locked, a wrong PIN counted, and that the GHCR image can be pulled anonymously, via the anonymous token GHCR issues, which is the exact failure Lightsail would hit on a private package. Verified: the image check returns 403 and fails for this still-private package and 200 for a known public GHCR image; against a production-mode instance with fake gateway credentials every site check passes; with the gateway variables empty it fails naming the variables to set. It submits one wrong PIN by design and says so. SUBMISSION.md and WRITEUP.md still recommended Render; both now describe the Lightsail container route, the CI build, and (in the write-up) the per-visitor PIN gate.

## Session: 2026-09-15 03:30
- **Branch:** main
- **Files changed:** DEVLOG.md, backend/scripts/bench-models.js, backend/src/db/seed.js, backend/src/llm/explain.js, frontend/src/pages/Activity.jsx
- **New files:** backend/src/llm/spend.js, backend/scripts/spend.js, docs/(Stan) MODEL SPEND.md
- **Notes:** TASK-93 and TASK-94. Spend tracking, at Stan's request: every paid call now has a running grand total in docs/(Stan) MODEL SPEND.md (2 calls, USD 0.0109 so far). Counting it exposed two undercounts in the audit trail it relies on: an explanation logged only its FINAL model call's tokens, so a three attempt explanation was recorded at a third of its cost, and an explanation that gave up after billed attempts logged nothing. Both fixed: tokens are summed across every call, and a failed explanation that spent anything writes an LLM_CALL row with failed: true (the Activity page says "attempted, no usable answer", and now also shows the model on successes, which it never did because it read the model from output instead of input). src/llm/spend.js prices those rows at Anthropic list price (Sonnet 4.5 USD 3 / 15 per million, checked 15 Sep; the gateway's global endpoint is standard price) and is shared by scripts/spend.js and the seed, which prints what it is about to erase because audit_log is the only record. Verified with a fake model: three calls summed to 2400/300 tokens where the old code logged 800/100, a failure after two billed calls recorded, a failure on the first call adds nothing, and totals match. The benchmark was rebuilt around the real pipeline: runExplanation is extracted from explainAlert (placeholders, retries, free-text fallback, verifier) and the benchmark runs it with only the model call swapped, because it used to build its own copy of the free-text prompt and never exercised the placeholder path judges see. It computes analytics in-process, adds --scenario reorder on a temporary SQLite backup copy, --only, and --save/--rescore so a change to a check is re-scored against identical answers. New semantic checks catch sentences wrong while every figure is real (arithmetic in "A exceeds B by C", low-stock alerts described as full, ordering suggested on too-much-stock alerts, a REORDER summary quoting a threshold other than the one it fired on). Each check was tested against the real 14 Sep failures, against correct sentences, and against every engine-written alert text (0 of 16 flagged); two rounds of false alarms were found by printing the offending sentence and fixed (negation such as "do not place any new orders"; any figure near "reorder point" rather than the one attached to it; the bare word "maximum"). Baseline, llama3, 8 alerts including the reorder scenario, 4 passes: 0% placeholder answers accepted first try, 2.59 model calls per explanation, 0 dangerous, 3 of 4 REORDER runs quoting the 250 MT policy reorder point against the 242 MT the alert fired on, 1 borderline slow-moving flag ("before placing another large order"), 88% usable. No paid calls were made.

## Session: 2026-09-15 04:30
- **Branch:** main
- **Files changed:** .kiro/specs/mvp1-inventory-visibility/design.md, .kiro/specs/mvp1-inventory-visibility/requirements.md, DEVLOG.md, backend/scripts/bench-models.js, backend/src/engines/alerts.js, backend/src/llm/explain.js, backend/src/llm/slots.js, backend/src/routes/inventory.js, docs/(Stan) MODEL SPEND.md, frontend/src/lib/explain.js, frontend/src/pages/Dashboard.jsx, frontend/src/pages/Inventory.jsx
- **New files:** none
- **Notes:** TASK-95, the REORDER explanation flaw, which turned out to be four disagreeing definitions of when REORDER fires. alerts.js compared inventory position against the CALCULATED reorder point, and requirements.md agreed; design.md, the rule-based explanation, the stock bars and the Inventory page's editable Reorder point field all treated the APPROVED value as governing; the Dashboard's Needs Attention built its own rule from available stock against the approved value only when cover was already below band; the projection chart drew its reorder line at the calculated value. Measured first: both choices raise identical alerts on the seeded demo and on the TW-25KG test edit, and diverge only where the two values are far apart (TJ-10KG approved 140, calculated 253). Stan chose the approved value, so editing the Reorder point now actually moves the alert. One rule everywhere: inventory position (available plus inbound) at or below reorder_point_policy, not a stockout, not idle. The alert message called the position "on-hand + expected incoming"; it is available stock (230, not the 290 on hand). The rule-based trace now leads with the approved point and drops the false claim that health status uses it (health uses days of cover). Both specs updated. AI layer: REORDER gets {inventory_position} and no longer gets {on_hand_stock} or {max_stock}; the on_hand description is neutral again because the 14 Sep overstock wording ("the figure the maximum stock level is measured against") leaked into a REORDER answer as "reaching its maximum capacity"; the REORDER brief names the rule; free-text answers now have the "Here's the explanation:" lead-in stripped, which was only done in placeholder mode. Benchmark, llama3, 32 runs each, re-scored with the same checks: contradictions 4 to 0, usable 88% to 100%, and REORDER explanations moved from free text at 3 calls to placeholders at 2. Two more false alarms in my own checks were found by reading the flagged sentences and fixed (arithmetic now positional around "by"). One Sonnet check (USD 0.0055, ledger total 0.0164): the old wrong threshold is gone, but it wrote "inventory position has reached 250 MT" when the position is 230, attaching {reorder_point} to the wrong phrase, and my checks missed it while flagging a correct "without carrying excess stock". Open, next: make comparisons structurally unwritable and promote measured checks into production.

## Session: 2026-09-15 07:30
- **Branch:** main
- **Files changed:** DEVLOG.md, backend/scripts/bench-models.js, backend/src/llm/explain.js, backend/src/llm/slots.js
- **New files:** backend/src/llm/semantic.js
- **Notes:** TASK-96, making explanation errors structurally hard and caught when they happen, done on free llama3 and fake-model harnesses only (no paid calls, ledger unchanged at USD 0.0164). Three layers. (1) Each alert type's trigger comparison is written by the system as the opening sentence ("X was flagged because its inventory position of 230 MT ... is at or below the approved reorder point of 250 MT"), and the separate placeholders it is built from are withheld, so a model cannot re-pair them; Sonnet's "inventory position has reached 250 MT" is now unwritable. A first version offered the clause as a required {alert_trigger} placeholder, which failed the format so often on llama3 that 28 of 32 answers fell back to free text at 2.91 calls, so the system writes it instead. Placeholder lists were trimmed per alert type after reading answers: min_order and safety_stock off stockout and reorder ("order at least that amount" against a suggested 597), overstock_amount off everything (IDLE wrote "write down the 18 MT" when 78 MT was idle), supplier and lead_time off IDLE and AGEING. (2) src/llm/semantic.js moves the benchmark's checks into production, folded into runExplanation as dangerous issues (retry, then reject to the rule-based trace): positional "A exceeds B by C" arithmetic, low-stock alerts described as full and ordering advised on too-much-stock alerts (with negation, including "before placing"), a figure attached to a named quantity (inventory position, on-hand, available, maximum, reorder point) matching its real value, a quantity attached to ordering matching the suggested order ("reorder 20 MT" when 597), and any claim that an action was already taken ("the supplier has been informed"), which contradicts the approve-every-action design. Tested against the real failures, correct sentences, all 16 engine-written alert texts (0 flagged) and 128 saved benchmark answers, where every remaining flag was a real error. (3) Two causes of wasted retries found by recording WHY attempts were rejected: every product name contains a digit ("Japonica Short Grain 5KG"), so the no-digits rule rejected answers that merely named the product, which is most of why llama3 needed 2 to 3 calls; product names and KG pack sizes are now exempt while figures are still rejected. And 15 placeholder values ("SGD $26.2K", "1 month and 23 days") held figures absent from the facts list, so the verifier called correct answers invented; it now also knows every value the system inserts (0 of 122 flagged; an invented 999 MT still caught). Also: any brace token that is not an exact placeholder name is rejected ({Product} used to reach the reader), ".:" joins and a doubled "was flagged because" are cleaned, and "Here's another attempt:" lead-ins are stripped. Result on llama3, 8 alerts including the reorder scenario, 4 passes: first-try acceptance 0% to 94%, model calls per explanation 2.59 to 1.06, placeholder path 13 to 32 of 32, free-text fallbacks 19 to 0, contradictions shown 3 to 0, latency 10.6 s to 4.3 s. On the paid tier 1.06 calls is under half the cost per explanation. A fake model deliberately making each known mistake is blocked in every case, a contradiction on every attempt is never shown and its billed calls are still counted, and a good answer for each of the six alert types is accepted on the first call. Ready for the Sonnet check Stan asked to hold until morning.

## Session: 2026-09-15 08:30
- **Branch:** main
- **Files changed:** DEVLOG.md
- **New files:** frontend/src/lib/explain.eli18-draft.js, docs/(Stan) ELI18 EXPLANATIONS DRAFT.md
- **Notes:** TASK-97, a concise plain-English (ELI18) draft of the rule-based Why? explanation, for Stan's review before anything changes; he chose draft-first overnight because this is copy judges read. The draft is a separate module with the same export as lib/explain.js and nothing imports it, so the app is unchanged and approval is a one-line import swap in pages/Alerts.jsx. One or two short sentences per step, jargon removed ("blended rate", "throughput", "disposition", "binding constraint"), plainer headings in the same four-step order, trend flags in everyday words. Drafting found a factual error in the LIVE stockout explanation: "55 days of cover is the minimum ... Cover is short of that by 17 days", where 17 is cover against the 45 day lead time (stockout_gap_days) and the gap to 55 is 27; the draft states each figure against what it measures, and the live text was deliberately left alone overnight and flagged in the review doc. The review doc is generated from the real code and live figures for all six alert types, not typed by hand: current versus draft side by side, word counts (19% to 46% shorter), and automatic checks that no explanation contains null/undefined/NaN or a dash and that every figure traces to the engine's data for that alert (clean). Alerts come from buildAnalytics rather than /api/alerts, which omitted the dismissed STOCKOUT_RISK and IDLE alerts. Reading the output found and fixed "plus 0 MT already ordered", the 90-day idle rule stated twice, and "decelerating" said twice. A stress test with empty and partial SKU records found "null is available and undefined MT sells each day" in three types; a safe() guard now drops any sentence built from a missing value (24 cases clean), a safeguard the live explanation lacks.

## Session: 2026-09-15 09:15
- **Branch:** main
- **Files changed:** DEVLOG.md, backend/src/llm/explain.js, backend/src/llm/slots.js
- **New files:** backend/scripts/sonnet-check.js
- **Notes:** TASK-98, the last gaps before the paid check, found by rehearsing it on free llama3. The approved action is now guaranteed rather than requested: stockout and ageing answers ended without stating it, so if the model's text does not contain alert.recommended_action the system appends it, the same move as the system-written opening sentence, with no retry. safety_stock is withheld from STOCKOUT_RISK after "The 10 days is insufficient to cover the time it takes for a new shipment to arrive" presented the buffer as cover. A fake model giving a one-sentence answer still gets the reason with its figures first and the action last, checks clean, for all six alert types. Benchmark, llama3, 8 alerts including the reorder scenario, 2 passes: 100% accepted first try, 1.00 model call per explanation, 0 fallbacks, 0 contradictions, every answer opening with the reason and ending with the approved action. scripts/sonnet-check.js makes the paid check one command: by default it prints the alerts and an estimate (about USD 0.035 for all six) and makes no calls; --dry-run rehearses the same run on llama3; only --confirm-spend uses the gateway, through explainAlert so the audit trail and scripts/spend.js account for it, and it prints the ledger rows to add. Verified only in its free modes, against a copy of the dev database: 0 gateway calls, paid total unchanged at USD 0.0164. Stan asked for no paid calls overnight; the check waits for him.

## Session: 2026-09-15 09:00
- **Branch:** main
- **Files changed:** DEVLOG.md, docs/(Stan) ELI18 EXPLANATIONS DRAFT.md, docs/(Stan) MODEL SPEND.md, frontend/src/lib/explain.js
- **Deleted:** frontend/src/lib/explain.eli18-draft.js
- **Notes:** TASK-97 applied. Stan approved the ELI18 draft, so it replaced lib/explain.js (moved over it, so git shows the wording diff) and the draft file is gone. This also fixes the live stockout sentence that paired a 17 day gap with the 55 day minimum. Checked in the browser on the REORDER alert, which found one more imprecision: "230 MT has reached the approved reorder point of 250 MT" when 230 is below it, now "is at or below", matching the alert's own rule. Stan also approved one paid Sonnet check, REORDER only (USD 0.0048, running total USD 0.0212, ledger updated). Accepted first try, the system-written opening and every figure correct, approved action present. Two weaknesses no check catches, noted for Stan rather than fixed: it said 52 days of cover "does not leave enough time" for a 45 day lead time (true only counting the 10 day safety buffer; the engine's stockout gap is 0), and it invented urgency ("avoids expedited freight costs", "Delay beyond today and the economics worsen quickly") that no figure supports.

## Session: 2026-09-15 10:00
- **Branch:** main
- **Files changed:** DEVLOG.md, backend/src/llm/explain.js
- **Notes:** TASK-99, the invented urgency found by this morning's Sonnet check ("avoids expedited freight costs", "delay beyond today and the economics worsen", and 52 days of cover "does not leave enough time" for a 45 day lead time). A new hard rule 9 in SYSTEM forbids costs, savings, fees, deadlines or urgency no figure supports. The REORDER brief had seeded it: "there is still time to order at normal freight rates" gave the model freight as a topic, so it now says a normal order can still arrive in time, that not enough time would be a stockout alert, and that the order is due, not urgent (within the lead-time window, never "as soon as possible"). Prompt only, no new check, because a check costs a paid retry each time it fires. llama3, reorder scenario: 4 passes before the brief change 97% first try, 1.03 calls; after, 94% and 1.06 (within run-to-run noise; a 2-pass run the same morning read 75% then 94%). REORDER answers saying "as soon as possible": 3 of 8 before, 0 of 4 after. Stockout answers still say "consider expedited freight", correctly, since that is the engine's own recommended action. llama3 never produced the Sonnet phrasing even before the fix, so only the paid run can confirm it; Stan has deferred all paid checks to one run near the 28 Sep submission close.
