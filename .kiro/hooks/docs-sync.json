{
  "version": "v1",
  "hooks": [
    {
      "name": "Docs sync reminder",
      "trigger": "AgentStop",
      "description": "When the agent finishes responding, reminds it to add a devlog entry (and update the tracker or spend ledger) if files changed since the last entry. Silent otherwise. Same script as the Claude Code Stop hook. Zero tokens.",
      "action": {
        "type": "command",
        "command": "sh .kiro/hooks/docs-check.sh",
        "timeout": 10
      }
    }
  ]
}
